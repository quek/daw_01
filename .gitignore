/target
/.claude/settings.local.json
/.claude/scheduled_tasks.lock
/.claude/worktrees/
# session-internal transient files (PostToolUse / Stop hook が書き出す)。
# session UUID が混ざるので glob で除外。
/.claude/.session_events.*.log
# pending reflection と、 SessionStart hook が mv で作る .last 消費済みマーカーの両方。
/.claude/.session_reflect_pending.md*
# in-process libav (rsmpeg) shared FFmpeg build (BtbN n7.1 lgpl-shared).
# Too large for git; fetched per machine. See docs/plan_video_export_libav.md.
/third_party/
# 個人用 FIXME リスト (編集禁止の作業メモ)。マシン固有なので追跡しない。
/docs/FIXME.md
