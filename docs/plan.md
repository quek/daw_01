理想とベストプラクティスを追求する。
そのためには大胆に破壊して作り直す。

# Rust 製・モデルを Clone しない DAW 向け GUI ライブラリ — 設計計画 (M9 = Real DAW Validation / M1-M8 履歴は history.md)

## Context

- **目的**: Rust で DAW (Digital Audio Workstation) のシェル UI を書くための **GUI ライブラリ** をゼロから設計・実装する。GUI のみが本ライブラリの責務、audio / IPC は別プロセス。
- **本ファイルの位置付け**: M9 (Real DAW Validation) の計画。M1-M8 + M5.5 の完了履歴・詳細設計・検証手順は [history.md](history.md) に分離。**正本 (canonical)** は git 管理下の `F:\dev\gui_01\docs\plan.md` (リネームで紐付けが切れる `~/.claude/plans/` のハッシュ命名は使わない)。
- **核となる制約 = 「モデルを Clone しない」** (load-bearing):
  1. MIDI クリップ・サンプル・オートメーションなど大きなデータを毎フレーム複製しない
  2. アプリ側がドメイン型を IPC で audio exe に伝送する際の都合 (zero-copy / 部分 snapshot) を library が阻害しない
  3. iced の `Application::Message: Clone` のような型境界をユーザのドメイン型に伝染させない
- **ライブラリの責務**: `&GuiModel` を借りて UI を描画 / 入力からエディット (`Edit<M>`) を収集し、`UiHost::frame(&mut model, ...)` で内部 apply + 自動 `request_redraw`
- **ライブラリの非責務**: GUI Model の所有・undo/redo の policy / audio exe との IPC・SHM / audio Model の設計

---

## アーキテクチャ要旨 (Hybrid: 即時モード API + 内部 scenegraph + heavy() 脱出口)

DAW UI の二極性 (静的密集型 vs 巨大可変型) をどちらも捌くため:

- 公開 API は **即時モード** に統一 (`derive` マクロ・Lens 不要、`Application::Message: Clone` 伝染なし)
- 内部実装で **scenegraph + input hash** により静的 UI の再描画コストを削減 (M4 で実装済み)
- `ui.heavy(id, |hctx| ...)` 脱出口でピアノロール・タイムライン・大量波形群などは retained-mode 風に最適化 (M5 で実装済み)

```rust
// 起動時: WindowBackend を渡しておけば、edits 検出時に自動で request_redraw が呼ばれる
let mut ui_host: UiHost<GuiModel> = UiHost::with_window(window.clone());

// AppHost::on_render() で 1 フレーム毎に呼ぶ
// (frame() が `&mut model` を取り、edits を内部 apply、必要に応じて redraw 自動要求)
ui_host.frame(&mut gui_model, &mut scene, screen, input, |m, ui| {
    ui.fader("master", m.master_volume, 0.7, "master fader", |v| {
        Edit::mutate(move |m: &mut GuiModel| m.master_volume = v)
    });
    ui.heavy("piano_roll", |hctx| { /* 巨大ビュー */ });
});
renderer.render(&scene)?;
app.notify_audio_process(&gui_model);

// audio thread に Edit を送る / batch apply / undo stack 等の advanced 用途では
// `frame_to_edits(&model, ...) -> Vec<Edit<M>>` を使う (利用者が apply タイミングと
// request_redraw を制御)。
```

**ライフタイム不変条件**: `Ui<'a>` の `'a` は `&'a M` 借用と一致 (`UiHost::frame` は `&mut model` を内部で `&*model` reborrow して `Ui<'a>` に渡す)。`Edit<M>` は `'static` Box 化されてフレーム末尾に内部 apply 消費。GAT 不要。

**「Clone しない」をどう守るか**:
- ユーザ Model 型に `Clone` / `PartialEq` / `Hash` / `Default` を要求しない
- メッセージ型を導入しない (Edit は `Mutate(Box<dyn FnOnce>)` か `Undoable { Arc<dyn Fn>, Arc<dyn Fn>, label }`)
- 内部 scenegraph の差分検出は **widget ID + プリミティブ末端値の hash** だけで行う
- derive マクロは禁止 (Lens 等)

---

## 基盤クレート選定 (M8 完了時点)

| レイヤ | 採用 | 採用バージョン / 状態 |
|---|---|---|
| Window/Event | winit | 0.30.13 |
| Rendering | 自前 wgpu パイプライン | wgpu 29.0.1 |
| Text | glyphon (cosmic-text + swash) | glyphon 0.11.0 |
| Layout | taffy | 0.10.1 |
| Platform handle | raw-window-handle | 0.6.2 |
| Math/binding | bytemuck | 1.25.0 |
| Async runtime (内部) | pollster | 0.4.0 |
| PNG 出力 (snapshot 用) | png | 0.17 |
| Native file dialog | rfd | 0.15 (M8、feature `dialog`) |
| OS clipboard | arboard | 3 (M8、feature `clipboard`) |
| 開発用 (bench / trybuild) | criterion / trybuild | M2 で導入 |
| 第 2 windowing (プラグイン) | baseview | **凍結** (Phase 44 完了後に再評価、rwh 0.5/0.6 互換待ち) |
| SVG / 任意 path 描画 | vello | **凍結** (Phase 44 完了後に再評価、現状 rect/glyph/line で全 example 成立) |

A11y (AccessKit) は **本ライブラリでは採用しない方針** (2026-05-02 ユーザ判断、個人 DAW プロジェクトのため再評価対象外)。

シェーダは現状 3 本構成: instanced rect / line strip (波形・メータ・オートメーション) / SDF glyph (glyphon 統合)。textured quad は M2-M8 で必要にならず未実装。

---

## 現状ワークスペース構成 (F:\dev\gui_01、M8 完了時点)

```
F:\dev\gui_01\
├── Cargo.toml                       # workspace, edition=2024, rust-version=1.95
├── rust-toolchain.toml
├── docs\
│   ├── plan.md                      # ★ 本ファイル (M9 計画)
│   └── history.md                   # M1-M8 + M5.5 履歴 + 詳細設計
├── crates\
│   ├── platform\                    # daw-ui-platform (winit抽象、raw-window-handle 経由 trait bound)
│   │   └── src\{lib,event,window,winit_backend}.rs
│   ├── renderer\                    # daw-ui-renderer (wgpu 29、自前パイプライン rect/line/glyph + OffscreenRenderer)
│   │   └── src\{lib,device,offscreen,scene}.rs + pipelines\{mod,rect,line,glyph}.rs + *.wgsl
│   ├── ui\                          # daw-ui-core (Ui/UiHost/Edit/history/shortcut/clipboard/dialog/popup/time/viewport/widgets/scenegraph/heavy)
│   │   └── src\{lib,edit,history,shortcut,clipboard,dialog,id,input,layout,popup,scenegraph,time,ui,viewport}.rs
│   │       └── widgets\{mod,button,checkbox,drag_rect,dropdown,fader,heavy,knob,label,level_meter,
│   │                    menu,scroll_area,split_view,tab_view,text_input,time_grid,
│   │                    waveform,automation}.rs
│   └── examples\                    # 9 example (M7 で daw_prototype 追加、M8 で全 example に shortcut/file drop 等を配備)
│       ├── mixer\                   # 8ch fader / button / IME
│       ├── waveform_validation\     # 128 widget LOD ストレステスト + ViewportState1D
│       ├── sample_editor\           # 選択範囲 + カーソル + RmsBars + ViewportState1D
│       ├── piano_roll\              # 100k notes + heavy() cached (5.77x) — M9 Phase 41 で note edit 統合予定
│       ├── arrangement\             # 500 widgets + heavy() cached (9367x)
│       ├── automation\              # cubic Bezier flatten + Catmull-Rom 点ドラッグ
│       ├── embedded_host\           # OffscreenRenderer で PNG snapshot (プラグイン UI 埋め込み実証)
│       ├── sample_edit_ops\         # 波形 trim / linear fade in/out + ViewportState1D — M9 Phase 42 で Undoable 化予定
│       └── daw_prototype\           # M7 visual prototype + M8 shortcut/file drop/dialog 統合
```

外部 project: `F:\dev\daw_01` (実 DAW プロトタイプ) が gui_01 を path 依存で利用中。Phase 44 で実利用フィードバックを集約。

---

## マイルストーン (M9 以降)

M1-M8 + M5.5 は完了済み (詳細: [history.md](history.md))。M8 完了時点 (commit 2ab8ab3) の主要状態:

- M7+M8 の widget / 入力基盤 一式完備: scroll_area / menu_bar / context_menu / dropdown / tab_view / split_view / time_ruler / bar_beat_grid / level_meter / history stack (Undoable) / shortcut / clipboard / drag&drop / multi-select (rect drag) / file dialog
- 9 example 全部動作確認済み (mixer / waveform_validation / sample_editor / piano_roll / arrangement / automation / embedded_host / sample_edit_ops / daw_prototype)
- daw_01 (実 DAW プロトタイプ) が gui_01 を path 依存で利用開始
- 設計の不変条件 (no-Clone / メッセージ型禁止 / derive 禁止 / audio・IPC 不混入) 維持

### M9 (Real DAW Validation — `Edit::Undoable` ergonomic 実証 + daw_01 conversation 統合) — ✅ 完了 (2026-05-04)

**目的**: M8 で導入した `Edit::Undoable` の ergonomic を、note 編集 / audio buffer 編集の 2 ケースで実証する。boilerplate が出れば library helper で吸収。daw_01 で並行検証して library API の fitness function を回す。

**動機**: `Arc<dyn Fn(&mut M) + Send + Sync>` ベースの Undoable は理論上は no-Clone を守るが、`Vec<MidiNote>` / `Vec<f32>` 級の **重い inverse** を要するケース (note multi-select delete、audio trim/fade) でユーザに boilerplate を強要しないかは未検証。「新しく入れた抽象は次の機会に使う」(`feedback_use_new_abstractions.md`) を Undoable に適用するタイミング。M9 を見送って theming / animation / 信号処理 widget に直行すると、Undoable の前提が崩れたまま機能拡張が積み重なるリスク。

**並行**: daw_01 から M7/M8 実利用フィードバックが 11 項目 (P0-P3) 届いている。詳細プランは [plan_daw01_feedback.md](plan_daw01_feedback.md)。Phase 41-44 の前提となる API 整備として P0-P1 を先行実施することが多い (例: P0-1 Shortcut::parse 記号受理は piano_roll の `Shift+/` shortcut で必要、P1-3 HeavyCtx delegate は piano_roll の rect-select で必要、P1-4 double-click は clip → Piano Roll タブ UX で必要)。

| Phase | テーマ                                                                 | 主な成果物                                                                                                                                                                                                                                                        | 状態                        |
|-------|------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------|
| 41    | piano_roll の note edit + multi-select 統合 + library widget 化 (主軸) | `Edit::with_inverse` / `Edit::snapshot_inverse` で note add/delete/move/resize/select、Ui::take_drag_rect_in_rect で rect multi-select、Ui::set_cursor 公開、`Ui::piano_roll` library widget + `NotesEditRequest` enum で API 完結                                | ✅ 完了 (41pre+a+b+c+d+e+f) |
| 42    | sample_edit_ops の trim/fade を Undoable 化                            | trim/fade in/out の 3 ボタンを `Edit::snapshot_inverse` 化。audio buffer の inverse 戦略は **混在採用**: trim = full snapshot (`Vec<Vec<f32>>` + viewport/selection/cursor)、fade in/out = 範囲 snapshot (`Vec<f32>` の e-s 個 + range + direction enum)          | ✅ 完了                     |
| 43    | debug overlay (validation 用)                                          | `Ui::debug_overlay(rect, frame_ms)` + `UiHost::last_frame_stats() -> FrameStats` で cache_hits/misses/widget_count/scenegraph_size/history_depth を画面右上に popup z-order の半透明 overlay。Ctrl+F1 を default shortcut binding (`debug_overlay_toggle`) に追加 | ✅ 完了                     |
| 44a   | popup pass の renderer pipeline 独立化                                 | Phase 43 で発見した「popup pass の prepare で base pass の rect/line/glyph buffer が上書きされる」問題を、renderer に `popup_rect / popup_line / popup_glyph` 3 つの独立 pipeline インスタンスを追加して根本解決                                                  | ✅ 完了                     |
| 44b   | Undoable ergonomic 評価 + 必要なら API 改善                            | call site 分類: `Edit::snapshot_inverse` 7 件 (Phase 41d で library 化済、ergonomic 良好) + fader/knob の drag release pattern 2 件 (3 件未満で premature abstraction 回避)。**追加 helper 不要、現 API で十分** を確定                                          | ✅ 完了                     |
| 44c   | daw_01 Note schema 統合 (f64 + lyric)                                  | **案 A 採用** (CLAUDE.md「大胆に破壊して作り直す」原則): gui_01 Note を `start_beat: f64` / `len_beats: f64` 化し、`lyric: Option<Arc<str>>` を追加。Phase 44b の API stability 確約は撤回。daw_01 が adapter なしで gui_01 widget を直接使える状態に     | ✅ 完了                     |
| 44d   | daw_01 conversation #002-#004 対応                                     | piano_roll の rect select を **Alt+drag → Shift+drag (加算)** に breaking 置換 (daw_01 旧自前実装慣習 + DAW 業界標準に合致)。`next = prev ∪ rect_inside` で「加算」を実現、排他は空白 click + Shift+drag の 2 ステップ。daw_prototype に **clip dbl-click → Piano Roll タブ遷移** デモを追加 (M9 P1-4 `take_double_click_in_rect` の活用例)。daw_01 conversation に 3 件返信 | ✅ 完了                     |
| 45a   | daw_01 conversation #008 — `Ui::panel` helper                          | `Ui::panel(id, rect, fill, radius)` + `Ui::panel_with_border(...)` を新設 (`crates/ui/src/widgets/panel.rs`)。`heavy + cached + push_rect` の boilerplate を 1 行に圧縮、cache key は rect/fill/border/radius の `[u32; 14]` で size/color 変化で自動 invalidate。daw_prototype の footer 背景を panel に置換 (利用例)。6 unit test pass | ✅ 完了                     |
| 45b   | daw_01 conversation #009 — `toggle_button_at`                          | `Ui::toggle_button_at(id, text, rect, value, &style, on_toggle)` + `ToggleButtonStyle { off_color, on_color, hint_band: Option<Color>, hint_band_h, border, border_width, radius, font_size, text_color }` を新設 (`crates/ui/src/widgets/toggle_button.rs`)。`button` と同じ armed-state click 判定、`value=true && Some(hint_band)` で rect 下端 hint_band_h px に色帯 (DAW M=赤 / S=黄 慣習)。`mixer` example の Mute checkbox を toggle_button + ON 時赤帯 hint band に置換。5 unit test pass (click → on_toggle(!value) / hint band 表示条件 / 色 swap) | ✅ 完了                     |
| 45c   | daw_01 conversation #006 — piano_roll velocity lane + playhead 内蔵    | `PianoRollView` に `velocity_lane_h: f32` (`0.0`=disabled) と `playhead_beat: Option<f64>` (`None`=disabled) を追加 (**breaking**、Default impl が無いため 3 caller を 1 commit で追従更新: `examples/piano_roll`, `tests/ui/pass/basic.rs`, 内部 `test_view`)。`PianoRollStyle` に `playhead_color` / `velocity_lane_bg` / `velocity_bar_color` / `velocity_bar_width_px` 追加 (Default 経由なので非 breaking)。velocity lane は `cached` 内、playhead は `cached` 外で毎フレーム描画。private helper `draw_velocity_lane` / `draw_playhead_line` を切り出し (Phase 45e arrangement で再利用予定)。piano_roll example で `playhead_beat: Some(2.0)` を有効化 (visible デモ)。5 unit test pass (vel_h on/off rect 差分 / velocity 0 skip / playhead in/out of range / playhead+vel 同時) | ✅ 完了                     |
| 45d   | daw_01 conversation #007 — `Ui::modal` + `Ui::list_view`               | `Ui::open_modal/close_modal/is_modal_open/modal()` を `popup_layer` 上の薄いラッパとして実装 (画面中央 panel 計算 + `update_popup_anchor` で毎フレーム更新 + ESC `take_shortcut("escape")` + outside click は popup_layer の標準挙動 + `on_close: Option<Box<dyn FnOnce() -> Edit<M>>>`)。`Ui::list_view` は `scroll_area` 上の薄いラッパで、表示 row 範囲のみ row callback (range skip 軽量 virtualization)、`row` callback は `&mut Ui<'_, M>` (P1-5 一貫)。`update_popup_anchor` ヘルパも追加 (anchor だけ更新、`prev_focus` 維持)。daw_prototype に footer "Demo Dialog" ボタン → modal + list_view デモを追加 (新抽象を次の機会に使う原則)。10 unit test pass (modal: open/close 切替 / ESC close + on_close / outside click close + on_close / overlay+panel 描画 / body 内 close、list_view: 全 row 呼び出し / 画面外 skip / hover+click 検出 / selected 描画 / empty list) | ✅ 完了                     |
| 45e   | daw_01 conversation #005 — `Ui::arrangement` widget (大物)             | 確定 API (`#005 [Replied]`) 通り `crates/ui/src/widgets/arrangement.rs` を新設 (~960 LOC、`Default` 付き `ArrangementView` / `ArrangementStyle` / `ArrangementResponse` で部分上書き ergonomic)。4 layer 統合実装: 描画基盤 (cached: ruler / lanes / clips / track selected bg / mute=赤・solo=黄 hint band。cached 外: selection overlay / drag preview / playhead / loop band) + drag (Move / ResizeLeft / ResizeRight、track 跨ぎ `MoveClipDelta`、Shift+drag rect select、commit-by-release、drag<16px は click 格下げ、double-click `DoubleClickClip` / `DoubleClickEmpty`、release frame の `pointer.pos` 不安定対策で `ClipDragSession.last_mouse` / `LoopDragSession.last_mouse_x` を drag 中各 frame で更新) + ruler (loop band Start / End / Middle / NewRange の 4 mode drag → `SetLoopRange` 発行) + header (`button_at` × 4 + `toggle_button_at` × 2 + Name button click → `SelectTrack` / Name dbl-click → `BeginRenameTrack` (UX 改善、background click も SelectTrack)、`Response.track_header_rects` を載せる)。lanes/ruler/header 内 hover 無で `set_cursor(Default)` (cursor reset)。`draw_playhead_line` を `crates/ui/src/widgets/playhead.rs` に `pub(crate) fn` 切り出し、piano_roll / arrangement で共有。`shortcut.rs` の default binding から無修飾 Arrow (`focus_up/down/left/right`) を削除 (text_input cursor キーが効くように)。daw_prototype の `draw_arrangement_tab` (92 LOC self-drawn) を `ui.arrangement(...)` 呼び出しに置換、`DawModel` に `arr_tracks` / `arr_view` / `arr_selected_clips` / `arr_selected_track` / `arr_rename_target` / `arr_rename_just_started` を追加し、`BeginRenameTrack` 受信で `arr_rename_target = Some(id)` + 初回 frame `ui.set_focus(text_input_wid)` → 該当 header rect 上に `text_input_at` 重ね描画 + Enter (`response.committed`) で確定 + ESC でキャンセル、on_change は track 名のみ更新 (overlay 消去とは分離)。track header 右クリックは `track_header_rects` ループで `context_menu_for(rect, ["Rename","Delete"], ...)` 発行。`arr_view.data_generation += 1;` を全編集 Edit で bump (cache 無効化)。trybuild 拡張 (`tests/ui/pass/basic.rs` で `Vec<ArrangementTrack>` / `Vec<ClipKey>` フィールドを追加、`ui.arrangement(...)` を全 17 variants の make_edit でコンパイル確認)。15 unit test pass (clip_to_rect / track_index_from_y / clip_hit Move / ResizeLeft / ResizeRight / 範囲外 / loop_band_hit_kind Start / End / Middle / 範囲外 / rects_intersect / Default 健全性 × 2 / drag_preview_geometry × 2) | ✅ 完了                     |
| 45f   | scene primitive call-order interleave (Phase 45e で発覚した z-order bug の根本修正) | 旧設計の `Scene { rects, glyph_areas, line_batches }` × `popup_*` (3 列 × 2) を **`primitives: Vec<Primitive>` × `popup_primitives` (1 列 × 2)** に統合。`Primitive::Rect/Glyph/Line` enum で call order を保つ。renderer は同 type 連続 primitive を 1 つの "run" にまとめて batch、各 run ごとに drawcall (state 切り替えコストは run 数 = 10-50 / frame で無視できる)。各 pipeline に `begin_frame` / `enqueue_run` / `upload` / `render_run` API を追加、`RectRun` / `LineRun` / `GlyphRun` で span/instance/pool index を保持。glyph は `glyphon::TextRenderer` の内部 buffer 上書き制約に対応するため `Vec<TextRenderer>` を pool 化、frame 内で run 数まで grow (allocate コストは grow 1 度だけ、shrink せず再利用)。`RunHandle` / `enqueue_runs` / `render_runs` を `pipelines/mod.rs` に共通化 (device.rs と offscreen.rs で DRY)。`scenegraph::CachedCommands` を `primitives: Vec<Primitive>` 1 本に統一。`ui::Ui` の `popup_rects/popup_glyphs/popup_lines` を `popup_primitives: Vec<Primitive>` に統一。test 用 helper (`scene.rect_count()` / `iter_rects()` / `rects_vec()` 等) を `Scene` に追加し、全 widget test を migration。これで **後から push した panel が button glyph の上に正しく描画**される (Phase 45e の text_input 背景透明 / clip drag が apply されない / cache 無効化されない bug が連鎖的に解消)。M9 Phase 44a で導入した popup pass 独立 pipeline は維持 (popup primitive を base pass 上に重ねる前提)                                | ✅ 完了                     |
| 45g   | daw_01 conversation #010 — `scroll_area` scrollbar drag bug fix         | `scroll_area.rs:108-117` で drag 判定用 thumb_rect を `offset = 0.0` で計算していたのを修正。**wheel 適用後の現在 `state.offset`** で thumb_rect を計算 (drag hit-test と描画で同一 rect を再利用する構造、乖離不能)。これにより scrolled 任意位置の thumb で drag 開始可。daw_01 側 (mixer / inspector / plugin_picker の scroll_area 利用箇所) は再ビルドのみで反映、コード変更不要。regression test `drag_starts_at_scrolled_thumb_position` を追加 (`UiHost::frame_to_edits` 4 frame シミュレート: wheel で offset 進める → scrolled thumb 位置で press → drag move + release → offset.1 > 200 を verify、旧 bug なら drag 不成立で 200 のまま) | ✅ 完了                     |

**設計判断 (M9 全般)**:

- **library widget 化は Phase 41e で完了** (2026-05-03 修正): 当初は「validation 段階で API を locked-down するとフィードバックが取りづらいため後回し」と決めたが、CLAUDE.md「理想とベストプラクティスを追求する。そのためは大胆に破壊して作り直す」方針に基づき、validation 中も breaking 変更を恐れず逐次反映する判断に変更。Phase 42-43 で発見した改善は widget API を breaking 変更で逐次反映する。daw_01 が gui_01 の Note 型を直接 import していないため、breaking 影響は軽微。
- **history group の API 形**: `begin_group("multi-delete") ... end_group()` の明示開閉と、`Edit::group(label, vec_of_edits)` の構造化のどちらが ergonomic かは Phase 41 で実証。
- **audio buffer inverse 戦略**: 案 A (full `Arc<[f32]>` snapshot) / 案 B (差分のみ Vec) / 案 C (`Arc<Vec<f32>>` COW) の 3 案を Phase 42 で検証、Phase 41 の Vec<Note> 戦略との整合性を基準に選定。
- **debug overlay は本格 prod tool ではなく validation 用**: 旧 M14 Phase 56 を validation 期間中に必要なので先行実装。
- **library helper の判定基準**: 「3 回以上繰り返される pattern なら library 吸収、それ以下は example に local helper を残す」を Phase 44 で適用。Phase 41-43 の Edit::with_inverse call site をすべて enumerate して評価。

**ergonomic 検証ポイント**:

- `Edit::with_inverse` 1 ブロックで note add/delete/move/resize が書けるか (Phase 41)
- multi-select delete で `Vec<Note>` を inverse に capture するとき `Arc<Vec<Note>>` で wrap が必須になる頻度 (`Fn + Send + Sync` 制約) (Phase 41)
- `begin_group / end_group` API が直感的に書けるか、Edit 発行ごとに group を開閉する形が冗長にならないか (Phase 41)
- 「重い data inverse」用に library helper (例: `Edit::snapshot_inverse(label, fwd, restore_from)`) を追加すべきか (Phase 42 / 44)
- Vec<f32> の Arc 共有時の `Send + Sync` 制約が問題になるか (Phase 42)

**完了条件 (DoD)** — すべて達成 (2026-05-04):

- ✅ Phase 41-43 のすべての `Edit::with_inverse` が library helper (`Edit::snapshot_inverse`) で吸収済 (Phase 44b で確認、3 件未満 helper 不要 ergonomic も verify)
- ✅ daw_01 で arrangement / piano_roll / mixer / inspector / browser 等の操作が gui_01 widget で動作 (Phase 44c-45g で会話ベース統合、conversation #001-#010 すべて [Replied])
- ✅ `cargo build --workspace` / `cargo test --workspace` (200 unit test) / `cargo clippy --workspace --tests -- -D warnings` / `cargo test -p daw-ui-core --test no_clone_required` 全 ✅
- ✅ `cargo run --bin piano_roll` で N notes 操作 → Undo / Redo 確認済
- ✅ `cargo run --bin sample_edit_ops` で trim / fade Undo / Redo 確認済
- ✅ 全 example で Ctrl+F1 → debug overlay toggle 動作
- ✅ `cargo run --bin daw_prototype` で arrangement / piano_roll / mixer / sample タブ + modal demo / clip drag / Rename overlay / scrollbar drag (Phase 45g) 動作

### M10 (Arrangement 機能拡張) — ✅ 完了 (2026-05-04)

**目的**: M9 Phase 45e で実装した `Ui::arrangement` widget の **欠けている daw 機能** を追加する。Phase 45e 動作確認で user 要望が出た 3 機能 (drag&drop track 並び替え / clip volume / 縦ズーム) を library API として shipping 確定する。

**動機**: M9 Phase 45e 完了時点で arrangement widget は「最小限の DAW timeline」(track header / clip drag / loop band / playhead) を提供するが、実用には:
- track 並び替えが ↑/↓ button のみで遅い (drag&drop が DAW 慣習)
- clip ごとの volume 調整ができない (DAW core 機能)
- track の縦ズームができない (画面が狭いとき不便)

これら 3 つは conversation #005 確定 API の **拡張** であり、API breaking 変更も恐れず 1 commit ずつで shipping (CLAUDE.md「破壊して作り直す」原則)。

| Phase | テーマ                                       | 主な成果物                                                                                                                                                                                                                                  | 状態      |
|-------|----------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------|
| 46    | drag&drop で track 並び替え                   | `ArrangementEditRequest::ReorderTracks(Vec<u32>)` 新 variant (新順での `track.id` 列)。`ArrangementResponse.reordering: Option<u32>` (drag 中 track id) 追加。`ArrangementStyle` に `reorder_drop_indicator` / `reorder_drop_indicator_h` / `reorder_drag_alpha` 追加 (Default 経由非 breaking)。`ArrangementState.track_reorder: Option<TrackReorderSession>` 追加 (anchor_track_id / anchor_index / anchor_mouse_y / last_mouse_y、release frame の pos 不安定対策で last_mouse_y を毎 frame update、既存 ClipDragSession と同パターン)。track header rect 上で M/S/Up/Dn/Del button rect 非 hit + primary press → reorder セッション開始、Name button area は include (drag start zone)。release で `dy >= 16px` なら `apply_reorder(cur_ids, anchor_index, target)` で新順発行、`< 16px` は click 格下げ (button_at の SelectTrack / Rename trigger に任せる)。drag 中は cached 外で **半透明 dragging row 複製** (`reorder_drag_alpha=0.6`、track_selected_bg を base color) と **横 line drop indicator** (header_pane + lanes 全幅、target row top に `reorder_drop_indicator_h=2px`) を float 描画。pure helper `compute_reorder_target_index(anchor_idx, mouse_y, header_top, track_top, row_h, n_tracks)` + `apply_reorder(ids, anchor_idx, target)` を切り出して 9 unit test (上端外 / 下端 clamp / self+next no-op / above keeps / below offsets / scroll / row_h=0 / apply 基本 / OOB safe)。`header_row_layout(row) -> HeaderRowLayout` 共通 helper を抽出 (per-track loop + reorder press detection で DRY)。daw_prototype に `ReorderTracks(order)` ハンドラ (id lookup + Vec rebuild、N_TRACKS=12 で O(n^2) 受容) と trybuild basic.rs に variant 追加 (1 commit で全 caller 追従) | ✅ 完了 |
| 47    | ~~clip volume 編集~~ → Phase 47b で再設計     | 当初 clip 底辺の volume band を実装 (commit 7f22500) したが、user 要望は **track header volume** だった (DAW 慣習: mixer fader と同じ位置)。clip gain は将来 `effective = track.vol * clip.vol` 乗算で再導入予定として revert (commit d7306ef) | ⏪ Revert |
| 47b   | track header volume slider                     | `ArrangementTrack.volume: f32` (`0.0..=1.0`、`1.0` で unity) + `ArrangementEditRequest::SetTrackVolume { track, prev, next }` + `ArrangementResponse.dragging_track_volume: Option<u32>` (Phase 47b commit f3603a6)。track header rect 内 buttons の **下に horizontal slider band** を描画 (`HeaderRowLayout.volume_band: Option<Rect>` で進歩的開示: `btn_h + band_gap + band_h <= inner.h` のときだけ表示、default `track_volume_band_h=4` で row_h >= 34 必要 = 32px では非表示、Phase 48 Alt+wheel 縦ズームで表示できる progressive disclosure)。`TrackVolumeDragSession` state (track_id / anchor_volume / band_rect / last_mouse_x、release frame の pos 不安定対策)。press 振り分け: volume band 内 → 最高 priority で TrackVolumeDragSession、その他で Name/row 背景 → reorder。drag 中は cached の band 描画と並行して preview volume を band fill 幅に反映 (display_v 切替の 1 panel 呼び出しでリアルタイム feedback)。release で `volume_from_mouse_x(last_mouse_x, band_x, band_w)` で 0..1 マップ + `SetTrackVolume` 発行。`ArrangementStyle` に `track_volume_band_h` / `track_volume_band_track` / `track_volume_band_fill` 追加 (Default 経由非 breaking)。pure helper `volume_from_mouse_x` を 3 unit test (basic / clamp 範囲外 / zero width safe) + `header_row_layout` band 表示判定 3 test (default 32px 非表示 / 34px+48px 表示 / band_h=0 disable) = +6 unit test。daw_prototype: `DawTrack.volume: f32` 追加 + `SetTrackVolume` ハンドラ + `track_row_h: 36.0` (band 即視のため) | ✅ 完了 |
| 47c   | track header の ↑/↓/× button 削除 + Delete shortcut で track 削除 | drag&drop reorder (Phase 46) + Delete shortcut で機能が重複したため、track header から `↑` (MoveTrackUp) / `↓` (MoveTrackDown) / `×` (DeleteTrack) buttons を削除。`HeaderRowLayout.buttons` を `[Rect; 5]` → `[Rect; 2]` に縮小 (M / S のみ残す)、`HeaderRowLayout.inner` field も未使用化したので削除。Name area が削減分 (5 buttons → 2 buttons = -72px) 広くなる。Delete shortcut handler を if/else 拡張: `selected_clips` 非空 → `DeleteClips`、空 + `selected_track` Some → `DeleteTrack`。`MoveTrackUp/Down` / `DeleteTrack` Edit variants は context_menu (Rename/Delete) / 将来の keyboard 用に **残存** (API 非 breaking、widget 内 emit のみ削除) | ✅ 完了 |
| 48    | 縦ズーム (`track_row_h` 動的変更)             | `ArrangementEditRequest::SetTrackRowH(f32)` 新 variant。**`Alt+wheel`** で track_row_h を `factor = (-dy * 0.005).exp()` で乗算して発行 (zoom_x と同じ exp curve)。既存の `Ctrl+wheel = SetZoomX` / `Shift+wheel = SetScrollX` / `plain wheel = SetTrackTop` と独立 modifier。daw_prototype: `SetTrackRowH(h)` ハンドラで `h.clamp(16.0, 96.0)` + `track_top` 上限再計算 + `data_generation` bump。integration test (UiHost::frame_to_edits + Alt 修飾 + scroll_delta) で SetTrackRowH 発火 + SetTrackTop 不発火 + exp curve で row_h 更新を確認。macOS の `Option+scroll = system 横スクロール` 衝突は将来 macOS 対応時に winit event 受信側で対処 | ✅ 完了 |
| 49    | track volume drag 中の live update + Undoable wrap (#011 症状 1) | daw_01 #011 で報告の「mixer fader (live update) と arrangement track volume band (release-only) の挙動非対称」を修正。`fader_at` (`crates/ui/src/widgets/fader.rs:215-237`) と同パターンを採用: drag 中の各 frame で `SetTrackVolume { prev: anchor, next }` Mutate 発火 (`TrackVolumeDragSession.last_emitted_volume` で同値発火を抑制) + release frame で `Edit::with_inverse` で Undoable wrap (forward = anchor→end / inverse = end→anchor、Ctrl+Z で 1 回 undo)。release frame の `pointer.primary_just_released` 判定で drag-中 Mutate を suppress (二重発火回避)。`make_edit` の trait bound に `Clone` を追加 (Undoable の forward/inverse 2 closure に分配するため、daw_prototype + trybuild basic.rs の closure literal は capture が自動 Clone なので追加対応不要)。daw_prototype 検証用に Arrangement タブ下部に `mini mixer strip` (各 track の `Ui::fader_at`) を追加 — `arr_tracks[i].volume` を共有する 2 つの UI が **drag 中も双方向リアルタイム追従** することを 1 画面で確認可能 (DAW 慣習で arrangement 上 / mixer 下) | ✅ 完了 |
| 50    | track reorder release frame の optimistic preview (#011 症状 2) | daw_01 #011 で報告の「Edit::ReorderTracks の deferred apply で release 直後 frame に旧順序の lanes が描画される 1 frame 遅延」を修正。release frame で `pending_reorder_order: Option<Vec<u32>>` を計算 → `tracks_for_draw: Vec<ArrangementTrack>` を新順序で組み立てて cached layer + per-track header loop の両方で使用 (release Edit は同じ `pending_reorder_order` を再利用、二重計算回避)。`viewport_key` に `pending_reorder_hash` を含めて release frame で cache miss 強制 (新順序での再描画)。tuple Hash 上限 (12 要素) 超えのため nested tuple `((existing 12 fields), pending_reorder_hash)` に変更。`pending_reorder_order` に含まれない track は元順序のまま末尾に keep (防御)。daw_prototype は連続再描画 (sim_phase アニメ) のため 1 frame 遅延の可視確認は困難だが、ロジックとして release frame で同 frame 内に新順序が反映される | ✅ 完了 |

**設計判断**:

- **drag&drop preview の見た目**: dragging track header を半透明 (`alpha 0.6`) でカーソル位置に追従 + drop indicator (横 line、target track の上下) を描画。release で `ReorderTracks(new_order)` 発行
- **clip volume の slider 位置**: 「clip 内 bottom band」 vs 「clip 上に重ねる外部 widget」の 2 案。前者を採用 (clip と一体感、外部 widget だと clip 数 = widget 数で重い)
- **縦ズームの modifier**: **`Alt+wheel`** を採用 (Ableton / Reaper 標準と整合、1 modifier で ergonomic、gui_01 内 Alt 未使用)。`Ctrl+Shift+wheel` (Bitwig 派生) は 2 modifier で却下

**完了条件 (DoD)** — すべて達成 (2026-05-04):

- ✅ Phase 46-48 全完了 (Phase 47 → 47b は user 要望で track header volume に再設計。clip gain は将来 phase で乗算追加予定)
- ✅ `cargo build --workspace` / `cargo test --workspace` (216 unit test) / `cargo clippy --workspace --tests -- -D warnings` / `cargo test -p daw-ui-core --test no_clone_required` 全 ✅
- 🔲 `cargo run --bin daw_prototype` で track drag&drop / **track header volume slider** / **Alt+wheel** 縦ズーム を実機確認 (user 側で実施)

### M11 (Library widget 拡充: drag-reorder list) — 進行中

**目的**: daw_01 conversation `#012 [Replied]` (2026-05-04) で要望された `Ui::reorderable_list` widget を新設し、汎用 drag&drop reorder を library API として提供する。daw_01 track_inspector の Chain section (MIDI FX / FX 内 plugin 順序入替) の `push_rect` 直呼びを置換可能にし、Phase 5 仕上げで残った「view layer の `push_rect` 直呼び 0 件」DoD を達成可能にする。

**動機**: M10 完了時点で gui_01 の list 系 widget は 2 種:
- `Ui::list_view` (M9 Phase 45d): 単純 scroll list (drag-reorder 内蔵せず、#007 で別 widget 路線確定済)
- `Ui::arrangement` 内蔵 track reorder (M10 Phase 46/50): timeline 専用、外に出ていない

両者の中間 = 「汎用 drag-reorder list」が欠落していたが、`#012` の要望と M10 で外出し済の pure helper (`compute_reorder_target_index` / `apply_reorder`) を組み合わせれば短工数で実装可能。

| Phase | テーマ | 主な成果物 | 状態 |
|---|---|---|---|
| 51 | `Ui::reorderable_list` 新設 | **公開型** (`ReorderableListStyle` / `ReorderableListResponse` / `ReorderableListEditRequest::Reorder(Vec<usize>)`)、`Ui::reorderable_list<T, F, R>` method (scroll_area wrapper + drag session state + commit-by-release で Reorder 1 度発行)。**`drag_handle_w`** parameter 対応 (`0.0` で row 全体 drag = Bitwig 風、`> 0.0` で row 左端 N px だけ drag 起点 = Logic / Cubase 風グリップ、残り領域は row callback の button_at 等が消費可)。**drop indicator** 横 line (target 位置に row 全幅、viewport clamp 付き)。**dragging row 半透明背景** (`row_bg_dragging`)。**release frame optimistic preview** (`pending_order` を 1 frame 保持して新順序で描画、Edit 適用 1 frame 遅延の visual 揺れを抑える、arrangement Phase 50 と同パターン)。`apply_reorder` を **`<T: Clone>`** に generic 化 (既存 `&[u32]` caller は単相化で無修正、`reorderable_list` は `&[usize]` で利用)。pure helper `compute_reorder_target_index` (anchor + target index 計算、id 概念無し) はそのまま流用。`make_edit: Fn(...) -> Edit<M> + Clone + Send + Sync + 'static` (M10 Phase 49 の `Ui::arrangement` と同 trait bound、Undoable Edit の forward/inverse 2 closure に分配するため `Clone` 必要)。**unit test 7 件** (visible row 数 / virtualization / 短 release で click 格下げ / drag release で Reorder 発行 / drag_handle_w 範囲外 press 無視 / drag_handle_w 範囲内 press / 空 list)。**trybuild basic.rs に reorderable_list 呼び出し追加** (no-Clone Model 制約 CI 固定)。**daw_prototype Demo Dialog に Plugin Chain demo** 1 セクション追加 (list_view デモの下、5 plugin chain を drag で並び替え) — memory `feedback_use_new_abstractions.md` 「新抽象は次の機会に使う」原則。`crates/ui/src/widgets/reorderable_list.rs` 669 LOC (test 含む) | ✅ 完了 |
| 52 | `Ui::text_input_at_focused` 新設 (daw_01 #013) | rename UI / inline edit の「メニュー → text_input 表示 → 即タイプ可能」(Logic / Bitwig / Cubase 慣習の F2 rename) を 1 関数で実現する `Ui::text_input_at_focused<F>(id, rect, text, on_change) -> TextInputResponse` を新設。`text_input_at` (既存) は不変 (breaking なし)。**初回 show 判定は frame counter なしに既存 `Scenegraph` の eviction 機構を活用**: `Scenegraph::contains(wid)` を pub method で出し、`Ui::was_widget_visible_last_frame(wid)` `pub(crate)` helper 経由で「前フレームに `with_widget_node` で描画されたか」を判定。`!was_visible_last_frame` のときに `set_focus(wid)` + `cursor_byte = text.len()` を実行 → caller 側の boolean flag 不要。完全に非表示 (フレーム飛ばし) → 戻ったときも再 focus される (= eviction で初回扱い)。**unit test 3 件** (初回 show で focus 取得 / 連続 visible では caller の手動 set_focus を上書きしない / 不可視 → 再表示で再 focus)。**trybuild basic.rs に呼び出し追加** (no-Clone Model 制約 CI 固定)。**daw_prototype の `arr_rename_just_started: bool` フィールド + 関連 boilerplate (BeginRenameTrack 2 箇所での set / overlay 描画箇所での `WidgetId::ROOT.child((b"text_input", &id))` 再現 + `set_focus`) を完全削除** し、`text_input_at_focused` 1 行に置換 — memory `feedback_pursue_best_practice` 「ユーザに workaround を強要する API は設計欠陥」と `feedback_use_new_abstractions` 「新抽象を次の機会に使う」を同時に満たす | ✅ 完了 |

**設計判断**:

- **`Reorder(Vec<usize>)` 元 index ベース**: chain plugin は識別子を持たない (`Vec<PluginInstance>` で index ベース管理) ので index が自然。`new_items[i] = items[order[i]]` semantics で commit-by-release で 1 度発行。stable id を持つデータ向けには将来 `key_of: Fn(&T) -> K` overload を追加する余地を残す (本 phase ではスコープ外)。
- **release frame `pending_order` を 1 frame 保持**: arrangement Phase 50 (track reorder) と同パターンで、release Edit が deferred apply されることによる「release 直後 frame で旧順序が描かれる」visual 揺れを抑える。次フレームで `take()` して消費する単純実装。
- **`drag_handle_w` を style に持たせる**: `0.0` (= row 全体 drag) を default にしつつ、`> 0.0` で row 左端だけを drag 起点にできる設計値。残り領域は row callback の button_at 等が click を消費する想定 (chain row の "GUI / ×" ボタン等)。
- **drop indicator は scroll_area 内で push_rect 直呼び**: drag float row (popup_layer 経由) は採用せず、scroll_area の closure 内で row 群と同じ z-order に描画する単純実装 (modal や popup の上に出ない、リスト内に閉じる方が DAW UX に整合)。

**完了条件 (DoD)**:

- ✅ Phase 51-52 完了
- ✅ `cargo build --workspace` / `cargo test --workspace` (226 unit test、+10 件 = reorderable_list 7 + text_input 3) / `cargo clippy --workspace --tests -- -D warnings` / `cargo test -p daw-ui-core --test no_clone_required` 全 ✅
- 🔲 `cargo run --bin daw_prototype` で Demo Dialog → Plugin Chain reorder + track header 右クリック → Rename → 即タイプ可能 (Phase 52) 動作確認 (user 側で実施)

### M12 (Performance optimization — perf_review_2026-05-04 P0 系) — 進行中

**目的**: 2026-05-04 の全体 perf review ([docs/perf_review_2026-05-04.md](perf_review_2026-05-04.md)) で発見された P0 critical 2 件を修正する。`Ui::with_widget_node` の cache hit/miss 経路で `Primitive` を Vec ごと毎フレーム clone している (Glyph に String / Line に Vec を内包) のと、`arrangement.rs` の `tracks_for_draw.clone()` が release frame 以外でも 2 度発火する問題。

**動機**: scenegraph cache の「cache hit が free」前提が、`Primitive::clone()` の中で String/Vec の二次 alloc が発生していたため崩れていた。1000+ widget のフレームで毎 hit ごと alloc が widget × primitive 数だけ発火し、heavy() で expecting する 60fps 維持が脅かされる状態。

| Phase | テーマ | 主な成果物 | 状態 |
|---|---|---|---|
| 53 | `Primitive` 内コンテナを `Arc` 化 (P0-1) | `GlyphArea::text: String` → `Arc<str>`、`LineBatch::segments: Vec<LineSegment>` → `Arc<[LineSegment]>` に **breaking 変更**。`Primitive::clone()` を refcount のみに圧縮し、`with_widget_node` の cache hit/miss 経路 (scenegraph) で発火する `cached.primitives.iter().cloned()` / `to_vec()` が String/Vec の二次 alloc 無しで済むようにする。renderer pipeline 側 (`area.text.hash()` / `&area.text` で `Buffer::set_text` / `batch.segments.iter()` 等) は `Arc<T>` の deref で全て無変更。`line.rs:174` の `for seg in &batch.segments` だけ `&Arc<[T]>` が `IntoIterator` でないので `batch.segments.iter()` に変更。`menu.rs:634` の `g.text.as_str()` は `Arc<str>::as_str` が unstable のため `g.text.as_ref()` に変更。構築側 (~30 箇所、widget / example / test 全部) は `text: foo.to_string()` → `text: foo.into()` (`&str → Arc<str>`) / `segments: my_vec` → `segments: my_vec.into()` (`Vec → Arc<[T]>`) のように `.into()` を 1 つ追加。すでに `Arc<str>` だった `ArrangementClip.name` / `note.lyric` は `clone()` で refcount bump に変更 (旧 `to_string().into()` の二重 alloc を撲滅)。daw_01 grep で直接構築箇所無しを確認、conversation 通知不要 | ✅ 完了 |
| 54 | `arrangement.rs` の `tracks_for_draw` 冗長 clone 撤廃 (P0-2) | `tracks_for_draw: Vec<ArrangementTrack>` → `Arc<[ArrangementTrack]>` 化。heavy() closure (`'static` 要求) と per-track header loop の両方で同じデータが必要だが、Arc にすれば 2 度目以降は **refcount のみ** で済むため、`tracks_owned = tracks_for_draw.clone()` の deep clone (N tracks × M clips per track の比例 alloc) を **撲滅**。1 度目 (`Arc::from(tracks)` または reorder build → `.into()`) は `'static` 要件で不可避 (Cow は `'static` ライフタイムで成立しない)。closure 内 (`draw_lanes_bg` / `draw_clips` / `draw_selection_overlay` / `draw_drag_preview` 等) は `&[ArrangementTrack]` を取るシグネチャのまま、`&tracks_owned` (= `&Arc<[T]>`) が deref coercion で `&[T]` に変換されるので無修正。`tracks_owned.len()` も Arc<[T]> deref で動く。daw_01 影響: gui_01 が export するのは `ArrangementTrack` 構造体のみで `tracks_for_draw` は private、breaking 影響無し | ✅ 完了 |

**設計判断**:

- **内部 field Arc 化** (現行) vs **variant payload Arc 化** (`Primitive::Glyph(Arc<GlyphArea>)`) の二択で前者を採用: (a) renderer 側 API が無変更、(b) `Rect` variant は Copy のままなので局所的、(c) `GlyphArea::clip_rect` 等の field 直接参照を保てる
- **`LineBatch: Default`** は維持: `Arc<[T]>: Default` が std で `Arc::from(&[])` 相当を提供しており、derive(Default) がそのまま動く
- **`tracks_for_draw` を Arc<[T]> 化 (Phase 54) 採用、Cow 不採用**: perf_review 当初案 (`Cow<'_, [ArrangementTrack]>`) は heavy() closure の `'static` 要件で成立しない。さらに per-track header loop が closure 後に同じ data を使うため、closure 内に move しきれない (closure と per-track loop の両方で owned コピーが必要)。Arc<[T]> なら 2 度目以降は refcount のみで P0-2 の目的 (=「2 度目の clone」撲滅) を達成

**完了条件 (DoD)** — すべて達成 (2026-05-04):

- ✅ Phase 53-54 完了
- ✅ `cargo build --workspace` / `cargo test --workspace` (226 unit test、Phase 52 から件数同じ = 既存 test の API 互換維持) / `cargo clippy --workspace --tests -- -D warnings` / `cargo test -p daw-ui-core --test no_clone_required` 全 ✅
- 🔲 `cargo bench -p daw-ui-core --bench scenegraph_cache` で cache hit frame の per-frame µs が削減されること (期待 30-70% 短縮、bench 環境差で揺れるため数値確認は user 側で実施)
- 🔲 `cargo run --bin daw_prototype` で arrangement track reorder / clip drag / volume drag の regression なしを目視確認 (user 側で実施)

### M13 (Library widget polish: time_ruler / bar_beat_grid 統合) — 進行中

**目的**: `Ui::arrangement` / `Ui::piano_roll` を library `time_ruler` / `bar_beat_grid` に乗せ替えて、daw_01 conversation #014 の 3 要望 (a) 小節番号テキスト表示、(b) `time_sig` 対応 grid (3/4・5/4・6/8 等で bar 線が正しい拍位置に出る)、(c) piano_roll の上部 ruler 領域新設 を 1 commit で達成する。CLAUDE.md「新しく入れた抽象は次の機会に使う」原則と memory `feedback_pursue_best_practice`「ユーザに workaround を強要する API は設計欠陥」を同時に満たす (daw_01 が「自前で別途 ruler を組む」回避策を強いられていた状況を library 内で吸収)。

**動機**: M7 Phase 27 で `Ui::time_ruler` / `Ui::bar_beat_grid` (拍/小節縦線 + 小節番号テキスト + `time_sig` 対応式 `numerator * 4 / denominator`) を library に追加していたが、`Ui::arrangement` / `Ui::piano_roll` widget は内部で独自に bar/beat 縦線を描画していた (= `b.rem_euclid(4) == 0` の 4 拍ハードコード、ruler に小節番号テキスト無し、piano_roll は ruler 領域自体無し)。library 抽象が arrangement / piano_roll でしか使い道がない上に、それらの widget が library 抽象を使っていない設計欠陥を解消する。

| Phase | テーマ | 主な成果物 | 状態 |
|---|---|---|---|
| 55 | `Ui::arrangement` / `Ui::piano_roll` を library `time_ruler` / `bar_beat_grid` に統合 (daw_01 #014) | `ArrangementView` / `PianoRollView` に **`bpm: f32` + `time_sig: (u8, u8)`** を追加、`PianoRollView` には **`ruler_h: f32`** も追加 (PianoRollView は Default impl 無いので breaking、3 caller を 1 commit で全更新)。`ArrangementStyle::ruler_label_color` / `PianoRollStyle::ruler_bg` + `ruler_label_color` を新設 (Default 経由で非 breaking)。`HeavyCtx::time_ruler` / `HeavyCtx::bar_beat_grid` delegate 追加 (cached layer 内で呼ぶ enabler)。`arrangement::draw_ruler_bg` 関数を削除し `hctx.time_ruler` に置換 (小節番号テキスト出る)。`arrangement::draw_lanes_bg` の bar/beat 縦線描画ループを削除し `hctx.bar_beat_grid` に置換 (3/4・6/8 等で bar 線が time_sig 対応)。`piano_roll::draw_grid_background` の (c) 拍縦線セクションを削除し `hctx.bar_beat_grid` に置換、レイアウトに ruler 領域を新設 (`grid.y = rect.y + ruler_h`、ruler は keyboard_w から始まる、`ruler_h: 0.0` で旧 piano_roll 互換)。**4 拍ハードコード `b.rem_euclid(4) == 0` を 3 箇所すべて撲滅**。viewport_key を **v2 化**、arrangement は 3 つ目の nested tuple として `(bpm, time_sig.0, time_sig.1)` を、piano_roll は `ruler_h.to_bits()` + 12 要素目に `(bpm, time_sig.0, time_sig.1)` 組タプルとして追加 (tuple Hash impl 12 要素上限に収める)。**library `time_ruler` の BarBeat label format を `mapping.format` (= "1.1", "2.1", ... 形式) から `format!("{bar_num}")` (= "1", "2", "3", ... 形式) に変更** (daw_01 #014 が期待する小節番号のみの表示、Seconds/SMPTE は `mapping.format` 経由のまま)。`ruler_h > 0.0` ガードで ruler を skip 可能 (旧 piano_roll 互換 + arrangement の test_view 互換)。**unit test 6 件追加** (arrangement: 3/4 で bar 線が 3 拍ごと / "1" "2" "3" ラベル / time_sig 切替で bar 線 set 変化、piano_roll: ruler_h=0 で label 無し / ruler_h=20 で "1" "2" 出る / grid bar 線の y_top が >=ruler_h)。daw_prototype `arr_view` / examples/piano_roll `view` / trybuild `basic.rs` の 3 caller を全更新 (`PianoRollView` Default impl 無し breaking)。**daw_01 への影響**: gui_01 commit と同期で `ArrangementView` / `PianoRollView` 構築箇所に `bpm` + `time_sig` (+ `ruler_h`) を追加する必要あり (daw_01 conversation #014 で受領後の追従指示を記入) | ✅ 完了 |

**設計判断**:

- **`bpm: f32` + `time_sig: (u8, u8)` を直接 view に持つ案 A** を採用 (`TimeMapping` を直接受ける案 B は不採用): daw_01 caller の薄さを優先 (`bpm: app.song.bpm, time_sig: app.song.time_sig` の 2 行で済む)。`sample_rate` は widget 内部で `48_000.0` ダミー値を合成 (BarBeat 表示の bar 線位置計算では比例定数として打ち消されるため不要、Seconds/SMPTE 切替時は将来別 API で受ける)。
- **`HeavyCtx` に `time_ruler` / `bar_beat_grid` delegate を追加**: `hctx.cached(viewport_key, |hctx| ...)` 内から `Ui::time_ruler` を直接呼べないため (HeavyCtx は Ui ではない)。既存 `label_at` / `button_at` / `context_menu_for` delegate と同パターン。`with_widget_node` のネスト (cached layer wid 内で time_ruler wid) は既存 `HeavyCtx::cached` 自身の `with_widget_node` 呼び出しと同じパターンで問題なし。
- **`ruler_h > 0.0` ガード**: arrangement / piano_roll 両方で ruler 領域が空のとき `time_ruler` 呼び出しを skip。これがないと ruler 内 tick 線がすべて bar_line color (= tick_color として共有) で出てしまい、cached layer 全体の primitive 数が増える + lanes/grid と区別できないノイズが入る。`piano_roll` は元々 ruler 無し (旧互換) なので必須、`arrangement` も既存 `ruler_h: 0.0` test_view との互換のため同じガードを入れる。
- **library `time_ruler` の BarBeat ラベル変更**: `mapping.format(s)` の出力 `"1.1"` (= bar.beat) は daw_01 #014 が期待する小節番号のみ "1", "2", "3" と異なる。`time_grid.rs` 内で `match mapping.display { TimeDisplay::BarBeat => format!("{bar_num}"), _ => mapping.format(s) }` に変更。Seconds/SMPTE 表示は引き続き `mapping.format` を経由する。daw_prototype piano_roll_tab デモ (= 唯一の既存 caller) でも "1", "2" 表示になるが visible な regression は無い (簡略化方向)。
- **piano_roll の ruler は `keyboard_w` から始まる**: keyboard 上に小節番号は出さず grid と同じ x 範囲のみ (DAW 慣習)。`arrangement` の `header_w` から ruler 始まりと平行。
- **piano_roll の viewport_key で `time_sig` を `(u32, u32)` 組として 1 要素化**: 既存 10 要素 + ruler_h + bpm + time_sig = 13 要素は tuple Hash impl 12 上限超過。`(view.bpm.to_bits(), u32::from(time_sig.0), u32::from(time_sig.1))` を 1 要素にまとめて 12 要素ぴったりに収める。
- **`PianoRollView` の Default impl は維持して追加しない** (M9 Phase 45c 既存設計): caller に明示的な値設定を強制する設計を継続、3 fields 追加で 3 caller を 1 commit 全更新する (M9 Phase 45c と同パターン)。
- **daw_prototype の piano_roll_tab はスコープ外**: 同関数は `Ui::piano_roll` を未使用 (生 `push_rect` + `time_ruler` 直呼びの単体デモ)、Phase 55 では触らない。`Ui::piano_roll` 化は `DawModel.notes` / `selected_note_ids` / `notes_generation` 追加が必要なので別 phase 候補としてメモ。

**完了条件 (DoD)** — すべて達成 (2026-05-04):

- ✅ Phase 55 完了
- ✅ `cargo build --workspace` / `cargo test --workspace` (**232 unit test、Phase 54 から +6 件**) / `cargo clippy --workspace --tests -- -D warnings` / `cargo test -p daw-ui-core --test no_clone_required` 全 ✅
- 🔲 `cargo run --bin daw_prototype` で arrangement タブの ruler に "1", "2", "3" 等の小節番号が出ること、time_sig (3, 4) 切替で bar 線が 3 拍ごとに移動することを目視確認 (user 側で実施)
- 🔲 daw_01 conversation #014 を `[Open]` → `[Replied]` に更新 (本 commit と同 sequence で別 step、`daw_01` ディレクトリでは commit しない)

### M14 (Modal popup の input masking + `button_at_clicked` — daw_01 #015) — 進行中

**目的**: daw_01 conversation `#015 [Open]` (2026-05-05) で報告された plugin_picker (`Ui::modal` + `Ui::list_view` で構築) の 2 件のバグを 1 commit で修正する。 (a) ✕ ボタンクリックが反応しない、(b) modal 内 list_view で wheel scroll が効かない。両者とも gui_01 側 root cause、API 2 点の追加で完結。

**動機**:
- (a) `button_at` の `on_click: FnOnce() -> Edit<M>` は `&mut Ui` を取れないため、click closure 内で `close_modal` を呼べない。Edit が daw_01 側 boolean を flip しても gui_01 内部の `open_popups` HashMap (popup state) は不変 → `modal.rs:87` の `is_modal_open` が true のまま → modal 描画継続。「click が無視される」と見える symptom の正体。menu item `m.item("New", |ui| { ui.push_edit(...) })` (M9 P1-5) で確立した「click handler が `&mut Ui` を取る」 pattern を button にも提供する必要があった。
- (b) daw_01 `root.rs:73` で `arrangement_view::draw` が `plugin_picker::draw` より先に呼ばれ、`arrangement.rs:1783` の `take_scroll_in_rect(lanes)` が pointer (modal panel 内) の scroll_delta を消費 → modal の list_view が呼ぶ頃には (0, 0)。modal panel と arrangement.lanes 矩形が overlap するため発生 (1280×720 で modal 中央 (640, 360) と lanes (440, 88, 840, 368) がかぶる)。同種の問題は `take_drag_rect_in_rect` / `take_double_click_in_rect` でも潜在 (今回は未報告だが将来必ず出る)。

| Phase | テーマ | 主な成果物 | 状態 |
|---|---|---|---|
| 56 | `button_at_clicked` 新設 + 全 take_* で modal-aware masking | **(a) `Ui::button_at_clicked(id, text, rect) -> bool`** を `crates/ui/src/widgets/button.rs` に新設 (`#[must_use]`)。click された frame で `true` を返す Edit-less 版。既存 `button_at` の本体をこちらに移し、`button_at` は `if button_at_clicked(...) { push_edit(on_click()) }` で 1 行 wrapper にリファクタ (DRY、hit-test 挙動の乖離防止)。menu item と同じ「click handler が `&mut Ui` を必要とする」pattern を button に提供 (modal の `close_modal` / `set_focus` / 複数 `push_edit` / 動的 popup 開閉)。**(b) `Ui::pointer_blocked_by_modal_popup()` helper** を `pub(crate)` で `crates/ui/src/ui.rs` に追加 — `drawing_in_popup` でない caller かつ pointer.pos が `open_popups` の `modal=true` な anchor 内にあれば `true`。`take_scroll_in_rect` / `take_drag_rect_in_rect` / `take_double_click_in_rect` 冒頭で `if self.pointer_blocked_by_modal_popup() { return /* zero/None */; }` で早期 return。modal popup の下に隠れている widget は pointer 入力を一切消費しない (overlay の意味が失われる問題を解消)。popup_layer 内 (modal の body) は `drawing_in_popup=true` なので通常通り消費可能。**unit test 5 件**追加 (button: press+release inside で click=true / press outside → release inside で click=false、ui: take_scroll で modal 下 (0,0) と popup_layer 内 (0,-3) の対比 / take_drag_rect で modal 下は drag 開始しない / take_double_click で modal 下は None)。`Ui::button_at` 既存 caller は無修正 (返り値追加のみ非破壊)、daw_01 plugin_picker.rs の ✕ ボタンは `button_at_clicked` + `close_modal` への書き換えが必要 (conversation #015 で daw_01 Claude に指示)。wheel 側は daw_01 側コード変更不要 (gui_01 修正のみで効くようになる) | ✅ 完了 |
| 57 | `text_input` の commit を NumpadEnter でも検出 (daw_01 #016) | DAW 数値入力 (BPM / time_sig / 拍数 / ピッチ等) でテンキー Enter を多用する業界慣習 (Cubase / REAPER / Logic 全部 numpad Enter で commit) に合わせる。`PhysicalKey` enum (`crates/platform/src/event.rs`) に **`NumpadEnter` variant を追加** (`Enter` 直後)。`winit_backend.rs::map_phys_key` に **`KeyCode::NumpadEnter => PhysicalKey::NumpadEnter`** マッピング追加 (旧実装は `PhysicalKey::Other(_)` に fallthrough → text_input の `_` arm で `\r` が control char filter で削られて何も起きなかった)。`text_input.rs:154-156` の commit 判定を **`PhysicalKey::Enter \| PhysicalKey::NumpadEnter => committed = true`** に拡張。`shortcut.rs::format_key` の exhaustive match に `NumpadEnter => "NumpadEnter"` arm を追加 (将来 shortcut binding に使う際の表示)。**unit test 2 件**追加 (text_input: NumpadEnter で committed=true / Enter でも引き続き committed=true 回帰防止)。Numpad0-9 / NumpadDecimal は winit 側で NumLock on 時に `KeyEvent.text = Some("0")` 等を emit するため text_input の `_ => ev.text` fallthrough で従来通り insert される (要 daw_01 側で NumLock on 時の動作確認、 NumLock off は数字入力外なので scope 外)。daw_01 側コード変更不要 (gui_01 path 依存再ビルドのみで効く) | ✅ 完了 |
| 58 | `text_input` の OS 標準 selection + Ctrl+A / Shift+Arrow / Delete / cut/copy/paste + shortcut layer の typing_focus 対応 + cursor / selection x 位置の実 advance 化 | **(a)** `TextInputState` に `anchor_byte` (selection の anchor 端) と `last_focused` (gained_focus 検知用) を追加。selection は egui `CCursorRange` 流の **anchor + cursor 2 点 (anchor==cursor で no-selection)** で表現。`prev_char_boundary` / `next_char_boundary` / `delete_range` を free fn 化。**(b)** `text_input_at` 内で「`was_focused == true && last_focused == false` で gained_focus」と判定し **anchor=0, cursor=text.len() で全選択** (click focus / programmatic focus / `text_input_at_focused` の 3 経路を 1 箇所で処理 = OS の F2 rename 標準挙動)。`text_input_at_focused` は `set_focus(wid)` のみ呼ぶ形に簡素化 (cursor 末尾設定は廃止、gained_focus path に委譲)。**(c)** キー処理を全部 `replace_range(min..max, new)` 1 形式に正規化 — 文字入力 / Backspace / Delete / IME Commit / Paste で selection あれば範囲削除 → insert、なければ単独操作。Shift+Arrow は anchor 固定で cursor のみ char 境界単位で動かして範囲拡張、修飾なし Arrow は selection あれば collapse (Left=min / Right=max)、なければ単独移動。**(d)** **`crates/ui/src/shortcut.rs` に `is_typing_only_shortcut(name) -> bool` を追加** (matches: `select_all` / `delete` / `cut` / `copy` / `paste`)。 `crates/ui/src/ui.rs` の `UiHost` に `last_typing_focus: bool` を追加し、`frame_to_edits` 冒頭の shortcut layer で **`last_typing_focus` が立っていれば** typing-only shortcut を `pending_shortcuts` に積まず `keyboard_events` に残す。frame 末尾で `last_typing_focus = typing_focus` を反映 (`focus_changed_in_last_frame` と同列の last-frame state)。**新 API: `Ui::take_typing_shortcut(name)`** — `keyboard_events` から `shortcut_map.matches(ev, mods) == Some(name)` の最初の Pressed event を消費して true。focus 中の text widget が `Ctrl+A` / `Delete` / `Ctrl+X/C/V` を取り出す統一窓口 (`shortcut.rs:209` の `M9 Phase 45e bug fix` 予告を解消)。**`heavy.rs` に delegate 追加**。**(e)** text_input の paste は `take_typing_shortcut("paste")` + `take_clipboard_paste()` で OS clipboard 経由、cut/copy は `set_clipboard_text(s)` で書き出し。`pending_clipboard_paste` 準備ロジックを **typing 中の paste shortcut も provider read を走らせる** よう拡張 (typing_paste_pending フラグ)。**(f)** `draw_text_input` を `(focused, cursor_byte, anchor_byte, preedit)` signature に変更。背景の上 / テキストの下に **selection 範囲の半透明矩形** (`Color::rgba(0.30, 0.50, 0.85, 0.45)`) を `push_rect` で描画 (focused かつ preedit 空のときのみ)。`input_hash` に `anchor_byte_for_draw` を含めて selection 状態変化で cache invalidate。**(g) cursor / selection x 位置を実 advance で計算 (M3 残作業の解消)**: ui crate に `cosmic-text 0.18.2` 直接依存を追加 (renderer の glyphon 0.11 が使う同 version で揃え)、新ファイル `crates/ui/src/text_metrics.rs` に `TextMetrics { font_system, scratch }` を新設して `measure_advance(text, font_size) -> f32` で 1 行 shape の line_w を返す実装。`UiHost.text_metrics` フィールド追加 + `Ui.text_metrics: &'a mut TextMetrics` + `Ui::measure_text(&str, f32) -> f32` API 公開。`text_input.rs` の `approx_text_width` (ASCII 7px / CJK 14px 固定概算) を **完全削除**、cursor / selection / preedit underline / IME request_ime 全箇所を `ui.measure_text(...)` に置換。proportional system font (Segoe UI 等) の "m" (~11px) や "i" (~4px) の実 advance に基づくので **pixel-accurate** に並ぶ (旧概算では `yfyfyfmmmmmmmmmmm` で 40-50px 単位の cursor ずれが出ていた)。renderer 側 `GlyphPipeline` の `FontSystem` とは別 instance だが、同じ system fonts を読むので shape 結果は一致 (キャッシュは別)。**unit test 12 件追加**: 全選択 → タイプで上書き / Ctrl+A → Delete で空文字列 / Delete (no selection) で 1 char 削除 / Backspace で範囲削除 / Shift+ArrowLeft で範囲拡張 → 'X' で末尾 char 置換 / Ctrl+C で text 不変 + clipboard 書込 / Ctrl+X で範囲削除 + clipboard 書込 / Ctrl+V で範囲を clipboard 内容で置換 / `typing_focus_blocks_global_delete_shortcut` (text_input focused フレームで `take_shortcut("delete")` が false) / TextMetrics: 空文字列 0 / 長い text の方が幅広 / 'm' は 'i' より幅広。**impact**: piano_roll / arrangement の `take_shortcut("delete")` は無修正で正しい挙動 (text_input focused 中は false / 非 focused 中は true)。daw_01 側コード変更不要 (gui_01 path 依存再ビルドのみで効く)。**break**: `text_input_at_focused` の挙動「初回 show で cursor 末尾」→「初回 show で全選択」(OS の F2 rename と一致、CLAUDE.md「破壊的 API 変更を恐れない」)。 | ✅ 完了 |
| 59 | `Ui::piano_roll` に歌詞 inline 編集 (text_input overlay) + モーラ単位の自動分配 (daw_01 #017) | VOICEVOX 歌唱 DAW 用に **note 上に text_input を重ね描き、L キーで起動、Enter で「あいうえ」 → 4 連 note へ "あ"/"い"/"う"/"え" 1 commit 分配 (1 undo)、Esc で cancel** を内蔵。**(a) `NotesEditRequest::SetLyrics(Vec<(NoteId, Option<String>)>)`** variant 追加 (1 commit = 1 Edit = 1 undo 単位、空文字列は widget 側で `None` に正規化)。 **(b) `PianoRollResponse` に `lyric_editing: Option<NoteId>` / `lyric_overflow_morae: usize`** 追加 (前者は app 側で「他 UI grey-out」 等の判断、後者は note 数 < モーラ数で捨てた数を status bar 表示等用)。**(c) `PianoRollStyle.lyric_edit_shortcut: Option<&'static str>`** 追加 (default `Some("piano_roll.edit_lyric")`、`None` で完全無効)。caller は `host.shortcut_map_mut().bind("piano_roll.edit_lyric", "L")` を 1 度呼ぶ。**(d) `PianoRollState.lyric_editing: Option<NoteId>`** 追加 (内部 state、編集中 note を frame 跨ぎで保持。Enter で次 note に遷移、note 削除時に defensive 自動 clear)。**(e) `pub fn split_into_morae(text: &str) -> Vec<String>`** を `crates/ui/src/widgets/piano_roll.rs` に新設 + `daw_ui_core::split_into_morae` で re-export (REAPER VOICEVOX script 準拠: 小書きかな ぁぃぅぇぉ ゃゅょ っ ゎ + カタカナ版 を直前 char と結合、それ以外は 1 char = 1 モーラ)。**(f) 内部 helper `collect_next_notes_for_lyric(notes, from_id, count) -> Vec<NoteId>`** で起点 note 以降を `(start_beat asc, pitch desc)` 順 (高音先取り、歌唱メロディと整合) に 取得。**(g) text_input_at_focused 接続**: id `("piano_roll_lyric", edit_id)` で auto focus + 全選択 (M14 Phase 57 gained_focus path)、Enter で `committed_text` を取り出して `split_into_morae` → `collect_next_notes_for_lyric` で分配先決定 → 1 つの `SetLyrics` Edit を `push_edit` + 次 note に遷移する `Select` Edit も同 frame で発行 (selection 連動)。**(h) `TextInputResponse.committed_text: Option<String>`** 追加 (Enter / NumpadEnter frame でのみ Some、 caller が `split_into_morae` 等で commit text を直接取り出せる、5 LOC 改修)。**(i) Esc 検知**: `take_shortcut("escape")` を piano_roll が editing 中に明示的に handle (1 frame で完全 cancel)、 text_input の `clear_focus_if_focused` も同時実施。 **(j) 編集中の input 短絡**: `editing_mode` flag で drag start / pending_click / `take_shortcut("add_note")` / `take_shortcut("delete")` / Shift+drag rect select を全 short-circuit。 **(k) shortcut layer 統合**: `is_typing_only_shortcut` に `"piano_roll.edit_lyric"` を追加 (編集中 typing_focus 立った frame では L key を text_input に通して `'l'` 文字入力可能、 編集外では global で 起動)。**(l) example/piano_roll**: `make_set_lyrics_edit(deltas: Vec<(NoteId, Option<String>, Option<String>)>)` factory 追加 (snapshot_inverse で undo 対応、 prev_lyric は dispatch closure で `m.notes.iter().map(|n| (n.id, n.lyric.clone()))` snapshot から引く)、 L キー bind と SetLyrics arm 追加。 **(m) trybuild test (`tests/ui/pass/basic.rs`)** に `NotesEditRequest::SetLyrics(_)` arm を追加して exhaustive 担保。 **unit test 16 件 + integration test 15 件 (計 31 件) 追加**: split_into_morae (10 件、ASCII / 拗音 / 促音 / 連続小書き / 先頭小書き defensive / カタカナ等)、 collect_next_notes_for_lyric (6 件、起点含有 / sort / 同 beat pitch desc / count 超過 truncate / id not found / count 0)、 widget integration (15 件、 L で起動 / 0 or 複数選択時 no-op / style.None で disable / 1 note + Enter で commit & clear / 4 note 分配 / 残あり次へ移動 / 拗音正しく分割 / overflow count Response / Esc cancel / 編集中 drag 短絡 / prefill commit / 空文字 → None / target 削除で auto-clear / 同 beat pitch desc 順 etc)。**impact**: daw_01 側で `AppEvent::SetNoteLyrics { clip_ref, lyrics: Vec<(u32, Option<String>)> }` 追加 + `make_edit` の SetLyrics arm 追加 + L キー bind の 3 点必要。daw_01 側 follow-up commit は user / 別 Claude 実施 (gui_01 から daw_01 へは編集禁止)。 | ✅ 完了 |
| 60 | piano_roll / arrangement widget に `SnapConfig` 引数を追加 (daw_01 #010) | **(a)** `crates/ui/src/snap.rs` 新設 (約 130 LOC): `SnapMode { Off, Straight{div}, Dotted{div}, Triplet{div}, Adaptive }` + `SnapConfig { mode, enabled, min_beat_unit }` + `pub const DEFAULT` (Adaptive ON + 1/128 floor) + `pub const OFF` + 4 method (`is_active(alt)` / `beat_unit(zoom)` / `snap_beat(raw, alt, zoom)` / `snap_beat_delta`)。 Adaptive 用 `MIN_VISIBLE_GRID_PX = 12.0` 内部 const。 `Eq` は `f64::min_beat_unit` のため derive 不可、 `PartialEq` のみ。 `crates/ui/tests/snap.rs` に 12 unit test 追加。 `lib.rs` で `pub use snap::{SnapConfig, SnapMode}` re-export。**(b) `Default` を `Adaptive ON` に決定** (#010 の「Default == OFF」案は不採用): 業界標準 (Cubase / Live) と整合、 library が「`enabled: true` を全 caller に書かせる」 workaround を強要する設計欠陥を回避 (memory: feedback_pursue_best_practice)。 `Default::default() == DEFAULT == Adaptive ON`、 `SnapConfig::OFF` で明示 raw 動作。**(c) `PianoRollView`/`ArrangementView` に `pub snap: SnapConfig` field 追加** (両者とも `#[non_exhaustive]` なし、 struct literal は 1 行追加で fix。 `PianoRollView` の `Default` は新設しない (silent default の罠回避)、 `ArrangementView` Default に `snap: SnapConfig::DEFAULT`)。**(d) drag overlay / release / Insert / DoubleClickEmpty に snap 適用**: piano_roll は L824 (drag overlay) / L1043 (Insert shortcut) / L1097-1149 (release Move/Resize)、 arrangement は L1287 (drag overlay) / L1577-1641 (release MoveClips/ResizeClips) / L1817-1820 (DoubleClickEmpty)。 複数選択 drag は anchor 0 の delta を `snap_beat_delta` で round → 全 anchor に同 delta 適用 (相対関係維持)。 Alt 押下は毎フレーム `pointer.modifiers.alt` で snap 一時無効化 (drag 開始時 snapshot ではない)。**(e) Resize の `min_len` を snap unit に合わせる**: `min_len = if is_active(alt) { unit.max(0.05) } else { 0.05 }`。 `drag_preview_geometry` 両 widget の signature に `min_len: f64` 引数を追加 (overlay と release で同一 clamp、 release 時の「カクッ」現象が消える)。**(f)** `clicked_at_beat_pitch` (piano_roll) / `clicked_at_track_beat` (arrangement) Response は **raw 保持** (caller 側が context_menu 等で独立判断、 必要なら caller が `view.snap.snap_beat(...)` を呼ぶ)。**(g) examples breaking**: `piano_roll` example と `daw_prototype` example の struct literal に `snap: SnapConfig::DEFAULT` 1 行追加 (デフォルト Adaptive で grid 吸着の動作確認 example として shipping)。 `tests/ui/pass/basic.rs` も 1 行追加。**(h)** 内部 widget unit test (`piano_roll.rs`/`arrangement.rs` の `test_view`) は **明示 `snap: SnapConfig::OFF`** で raw beat 値検証を保護 (Adaptive ON だと既存 test の期待値ズレ)。**unit test 12 件追加** (Default = Adaptive ON / OFF inactive / disabled returns raw / alt bypass / Straight 16 quarter snap / Triplet 4 unit / Dotted 8 unit / Adaptive zoom 4/64/1600 / min_beat_unit floor / negative delta / zero / 12px boundary)。**impact**: gui_01 widget が drag overlay 段階で grid 吸着、 commit 値も snap 済み。 daw_01 が local `snap.rs` (306 LOC) を `daw_ui_core::{SnapConfig, SnapMode}` に置き換え可能 (UI dropdown 等の daw_01 専用部分は残す)。 daw_01 conversation `#010 [Open]` → `[Replied]` に更新 (gui_01 から daw_01 へは commit しない、 file 編集のみ) | ✅ 完了 |
| 63b | `SnapMode::Straight / Triplet / Dotted` を DAW 業界標準 (1/4 = 1 beat = quarter note) に統一 (daw_01 #015) | **背景**: M14 Phase 60 で `SnapConfig::beat_unit` の Straight / Triplet / Dotted 計算を `1.0 / div` / `(2.0/3.0) / div` / `1.5 / div` で実装したが、 daw_01 #014 の動作確認中に user から「`1/4` が 1 beat になっていない、 `1/1` は 1 bar では？」 と指摘。 業界標準 (Cubase / Live / Reaper / FL Studio / REAPER manual) では label `"1/N"` は **N 分音符 (Nth note)** を表し、 quarter note (1/4) を 1 beat の基準とする (= MIDI ticks per quarter note の慣行と整合)。 現状は `1/4 → 0.25 beat` で user 期待 `1.0 beat` と乖離。 `crates/ui/src/snap.rs:86-88` の **3 行** で確実に直せる仕様 bug (#013 popup hardcode と同レベルで根本)。 **(a) 計算式変更**: `Straight { div } => 4.0 / div` (whole note = 4 quarter notes 由来の係数 4.0、 div=4 → 1 beat)、 `Triplet { div } => (8.0 / 3.0) / div` (4 * 2/3、 div=4 → 0.6667 beat)、 `Dotted { div } => 6.0 / div` (4 * 1.5、 div=4 → 1.5 beat)。 `Bars { count }` (time_sig 依存) と `Adaptive` (`beat_unit_for_zoom`) は **不変** (Adaptive の候補値 1, 0.5, 0.25, ... は元から「1 beat = quarter note」 semantics に整合)。 **(b) doc comment 全面更新**: `crates/ui/src/snap.rs` 冒頭に「単位の semantics」 セクション追加 (whole/half/quarter/eighth/sixteenth/32nd note の対応表)、 `SnapMode` 各 variant の doc comment を 1/4 = 1 beat の semantics に揃える、 `Bars { count }` との分岐 (4/4 では同値、 3/4 / 6/8 では分岐) も明示。 **(c) test 数値更新 (assertion 結果値は偶然不変が多い)**: `crates/ui/tests/snap.rs` で `triplet_4_unit` と `dotted_8_unit` の expected unit 値のみ更新 (assertion 計算式: `(8.0/3.0/4.0)` / `0.75`)、 `straight_16_snaps_to_grid` / `snap_beat_delta_negative` は 1.234 / 0.25 の round 結果が偶然 1.25 で **assertion 不変**。 `crates/ui/tests/alt_drag.rs` の absolute snap test 4 箇所 (arr / pr の short_drag + off_grid_anchor) は `pivot+raw を 0.25 grid で round` の結果が変わるので expected を更新 (`4.125 → 4.25`、 `4.5625 → 4.5`)、 `grid_unit = 1.0/16.0 → 0.25_f64`。 **(d) 新規 test +8 件**: `straight_4_is_one_beat` / `straight_8_is_half_beat` / `straight_1_is_whole_note` / `straight_16_is_quarter_beat` / `triplet_4_is_two_thirds_beat` / `dotted_4_is_one_and_half_beat` / `straight_div_1_vs_bars_count_1_at_3_4_diverges` / `straight_div_1_vs_bars_count_1_at_4_4_match` (DAW 業界標準対応 + Bars vs Straight 併存意義の明示固定)。 **API 不変**: `SnapMode` enum / `SnapConfig` struct / `beat_unit` / `snap_beat` 全て signature 同じ、 daw_01 側コード修正不要 (gui_01 path 依存再ビルドのみで効く)。 **既存 default 値の意味が user 期待値どおりに正常化**: `CHOICE_PIANOROLL_DEFAULT = 4` (label "1/16") は 旧 0.0625 beat → 新 0.25 beat snap (= 16 分音符)、 `CHOICE_ARRANGE_DEFAULT = 2` (label "1/4") は 旧 0.25 beat → 新 1.0 beat snap (= 1 拍単位)、 piano_roll の過敏すぎる挙動 + arrangement の細かすぎる挙動が両方解消。 daw_01 conversation `#015 [Open]` → `[Replied]`。 | ✅ 完了 |
| 62 | dropdown / menu_bar / context_menu_for の popup 画面はみ出し対応 (daw_01 #013) | **背景**: M14 Phase 61 の `SnapMode::Bars` 追加で daw_01 SNAP_LABELS が 18 → 21 件になり、 piano_roll toolbar (画面下から ~264px) の snap dropdown popup (21 × 24 = 504px) が画面下端からはみ出して末尾項目 `Adaptive` 等が選択不可になっていた。 `dropdown.rs:95-99` の popup_rect 計算が `Ui::screen()` を参照せず auto-flip / clamp のいずれも未実装が root cause。 同根の問題が `menu_bar` (menu.rs:234-241) / `context_menu_for` (menu.rs:498-509) にも存在していた (popup_layer を使う 3 widget 全部)。 **(a) 共通 helper 2 関数追加** (`crates/ui/src/popup.rs` を 30 LOC → 約 200 LOC に拡張): `popup_rect_below_or_above(anchor, popup_w, popup_h, screen) -> Rect` (dropdown / menu_bar 用、 下 → 上 flip → 大きい側 clamp の 3 段優先、 popup_h は据え置き)、 `popup_rect_clamped_at(origin, popup_w, popup_h, screen) -> Rect` (context_menu_for 用、 flip せず xy clamp のみ = DAW 標準右クリック挙動)、 内部 `clamp_x(origin_x, popup_w, screen_w)` private helper。 **(b) dropdown.rs の anchor 計算を汎用 union に書き換え**: 旧 anchor.h は `(rect.y + rect.h + popup_rect.h) - rect.y` で上 flip すると壊れる式だったため、 menu.rs の `union_rect` ヘルパと同等の `min/max(rect, popup_rect)` 式に統一。 outside_click 判定が flip 後でも body と popup の両方を「内」 として扱える。 **(c) menu.rs の menu_bar / context_menu_for を helper 呼び出しに置換** (各 1 箇所、 2-3 行差し替え)。 **採用判断**: 案 A (auto-flip 単独) + 座標 clamp = **「A + clamp」** (案 C の scroll は実装重い、 piano_roll の 504px popup は上 flip で完全救済できるため A の補強で十分、 scroll は将来 issue へ送り)。 sub-menu cascade (menu.rs:404-410) も同根だが daw_01 で踏まれていないため scope 外。 **単位の透過性**: gui_01 全体が **physical pixel ベースで統一** (Ui::screen() / widget Rect / pointer.pos) で scale_factor は ui crate 内で参照しないため、 helper は単位変換不要 (modal.rs:93-94 と同パターン)。 **unit test +8 件**: 通常下開き / piano_roll 21 items 上 flip case / 上下両方 NG (下広い → 下に置く / 上広い → flip + 上端 clamp) / 右端 clamp / context_menu 通常 / 画面下端 clamp / popup_h > screen_h 極端 case。 daw_01 側コード変更不要 (gui_01 path 依存再ビルドのみで効く)。 daw_01 conversation `#013 [Open]` → `[Replied]`。 | ✅ 完了 |
| 63b | arrangement widget の group hierarchy + multi-select + drag reparent (daw_01 #016) | **背景**: daw_01 PR2 (Reaper folder / Live group 互換、 `Track::parent_id: Option<u32>` 単一表現で TrackKind 廃止) の GUI 側対応。 階層インデント描画 / 折り畳み (▼/▶ disclosure) / 階層 1 段の indent / Shift/Ctrl による multi-select / track header drag-and-drop reparent の 5 機能を 1 commit で導入。 **API 拡張**: (a) `ArrangementTrack` に `parent_id: Option<u32>` / `depth: u8` / `collapsed: bool` の 3 fields 追加 (caller が depth を計算して渡す前提、 widget 描画毎の BFS を避ける)。 (b) `ArrangementStyle` に `indent_px: f32` (default 16.0) / `track_group_bg: Color` / `disclosure_color: Color` の 3 fields 追加。 (c) `Ui::arrangement` の signature を `selected_track: Option<u32>` → `selected_tracks: &[u32]` に breaking change。 (d) `enum SelectModifier { Single, RangeFromAnchor, Toggle }` 新設 + `ArrangementEditRequest::SelectTrack { prev: Vec<u32>, next: Vec<u32>, modifier: SelectModifier }` に置換 (modifier-aware を widget 内で decode、 caller boilerplate 削減)。 (e) `ToggleGroupCollapsed(u32)` / `SetTrackParent { tracks: Vec<u32>, parent: Option<u32>, anchor_after: Option<u32> }` 新 variant 追加。 SetTrackParent は **drag drop で常に発行され**、 parent 変更 + 挿入位置 (anchor_after = drop target track id、 None で先頭) を 1 Edit で表現する設計 (旧 `ReorderTracks` への分岐は廃止、 同 parent reorder も SetTrackParent で表現できる）。 **Widget state**: `ArrangementState` に `selection_anchor: Option<u32>` 追加 (Single/Range click で update、 Toggle は据え置き)。 `TrackReorderSession` に `source_track_ids: Vec<u32>` 追加 (multi-select drag で複数 track 一括移動)。 **描画**: track header に `depth * indent_px` の左 indent + group track には ▼/▶ disclosure アイコン (font_size = `track_text_size`)、 group 行には `track_group_bg` 背景。 `compute_visible_indices` helper で collapsed 親配下 (parent chain で collapsed=true がある track) を skip → header / lane (clip) 両方の row 計算が visible-domain で揃う。 `is_group_track` 判定は **caller の full `tracks`** から 1 度計算した `is_group_set: HashSet<u32>` で行う (visible filter で children が消えても group 判定が安定、 collapsed 後も ▶ 描画が継続して un-collapse 可能)。 **Hit-test / Event**: track header click は `pointer.modifiers.shift/ctrl` から SelectModifier を decode し、 SelectTrack を loop 後 1 度発行 (anchor 更新は Single/Range のみ)。 disclosure 領域 click は priority 高で ToggleGroupCollapsed 発行 (SelectTrack より優先)。 track header drag drop は dist >= 16px で release frame に SetTrackParent emit、 drop target に応じた `anchor_after` 計算: group 上は last descendant id (or group 自身)、 通常 track は top half=前 visible track id / bottom half=target id、 空白は visible top-level の最後 (or None)。 release frame の optimistic preview は廃止 (parent 変化は次 frame で反映、 sibling reorder を SetTrackParent 統合した代償、 1 frame の表示遅延)。 **daw_prototype**: `arr_selected_track: Option<u32>` → `arr_selected_tracks: Vec<u32>`、 `arr_collapsed_groups: HashSet<u32>` 新設、 `DawTrack::parent_id` 追加、 demo として track 0 = `Group A` (parent), 1-2 = children に設定。 SetTrackParent arm は (1) source remove (2) parent_id update (3) anchor_after の直後に insert の 3 段で arr_tracks を再構築 (sibling reorder + parent 変更 + multi-select 一括移動を全部カバー)。 ToggleGroupCollapsed arm は HashSet toggle。 DeleteTrack arm は子の orphan 防止で parent_id を None に reset。 **trybuild + tests**: `tests/ui/pass/basic.rs` の Model に `arr_selected_tracks: Vec<u32>` 追加、 SelectTrack / SetTrackParent / ToggleGroupCollapsed の match arm 追加。 既存 `crates/ui/tests/alt_drag.rs` / `arrangement.rs` mod tests の ArrangementTrack literal に新 3 fields 追加 + `selected_track: None` を `&[]` に書き換え (signature 更新)。 **新 unit test +7 件**: `is_group_track_returns_true_when_child_exists` / `is_visible_track_returns_false_when_ancestor_collapsed` / `compute_visible_indices_skips_collapsed_subtree` / `disclosure_rect_within_name_rect_left_edge` / `select_modifier_single_replaces_selection` / `select_modifier_toggle_adds_or_removes` / `arrangement_style_has_indent_and_disclosure_defaults`。 cargo test workspace 全 326+他 OK、 cargo clippy clean。 daw_01 conversation `#016 [Open]` → `[Replied]`。 daw_01 follow-up: `arr_selected_track: Option<u32>` → `arr_selected_tracks: Vec<u32>`、 `arr_collapsed_groups` 新設、 `DawTrack::parent_id` 追加、 SetTrackParent arm 3 段再構築、 等の breaking 対応 (gui_01 path 依存再ビルドだけでは build 失敗するため caller 側更新必須、 詳細は daw_01 conversation #016 reply 参照)。 | ✅ 完了 |
| 63a | popup overlay が外側 `with_clip_rect` の `current_clip` で clip される regression 修正 (daw_01 #014) | **背景**: M14 Phase 62 で `popup_rect_below_or_above` を入れて popup の位置計算は正しくなったが、 daw_01 daw_gui の piano_roll snap dropdown は **click しても popup が一切表示されない** 別 regression を起こしていた (arrangement は元から正常)。 root cause: caller が `with_clip_rect(pane_rect, |ui| ..)` (`tab_view` が pane を clip する経路) 内で `Ui::dropdown` を呼ぶと、 popup_layer 内の `push_rect` / `push_text` / `push_lines` (`crates/ui/src/ui.rs:770-797`) が `merge_clip(self.current_clip, cmd.clip_rect)` を **`drawing_in_popup` の真偽に関係なく** popup primitive にも適用していた (`current_clip = pane_rect`)。 popup_primitives は frame 末尾で base scene の後に extend されて popup pass で z-order 最前面に描画される設計だが、 各 primitive 自身が `clip_rect = pane_rect` を持つため renderer が pane_rect の外を全部 clip → 画面に popup が一切出ない。 piano_roll で出ず arrangement で出る分岐は **caller 側 `with_clip_rect` 配置の差** (piano_roll は bottom panel タブ内 = clip 有り、 arrangement は root pane 直下 = clip 無し)。 daw_01 conversation `#014` で提示した仮説 A (event consume) / B (z-order) / C (input handler consume) / D (上端張り付き) は **全部外れ**、 真因は **popup primitive の clip_rect** だった。 **修正**: `popup_layer` entry で `current_clip` を `None` に一時退避、 退出時 restore。 「dropdown を `with_clip_rect` の外で呼べ」 という制約を全 caller に強いるのは設計欠陥 (memory: feedback_pursue_best_practice) なので、 **popup overlay は z-order 最前面の modal として base scene の clip 制約から免除される** 不変条件を gui_01 側で吸収。 dropdown / menu_bar / context_menu_for / modal の **4 widget 全部** (popup_layer 経由) に同時に効く (DRY)。 Cubase / Live / 一般 GUI toolkit と同 semantics。 **regression test +1 件**: `popup_primitives_not_clipped_by_outer_with_clip_rect` を `ui.rs` mod tests 末尾に追加、 `with_clip_rect(pane_rect, \|ui\| popup_layer("p", \|ui\| push_rect(..)))` で `scene.popup_rects_vec()[0].clip_rect == None` を assert。 daw_01 側コード変更不要 (gui_01 path 依存再ビルドのみで効く)。 daw_01 conversation `#014 [Open]` → `[Replied]`。 | ✅ 完了 |
| 64 | `Ui::piano_roll` の velocity lane に drag 編集を追加 (daw_01 #018) | **背景**: M9 Phase 45c で velocity lane の **描画は内蔵済** (各 note の start_beat 位置に縦 bar、 高さ = velocity/127) だが、 lane 内の pointer 入力は widget が消費せず素通り、 caller も bar の hit-test ができない (widget が body 全体を取る) ため **マウスで velocity を変える手段が無い** 状態だった。 旧 vizia 時代の piano_roll_view が独自実装していた drag 機構が gui_01 widget 化 (M9) で引き継がれず描画のみが残っていた経緯。 **API 拡張**: (a) `enum NotesEditRequest::SetVelocity(Vec<VelocityUpdate>)` 新 variant 追加 + `pub type VelocityUpdate = (NoteId, u8)`。 release frame で 1 batch 発行 (Move/Resize と同 pattern)、 値は `0..=127` clamp 済。 prev は持たない (絶対値 mode で差分計算不要、 caller が undo 用に snapshot 保持)。 (b) `PianoRollResponse.velocity_dragging: bool` 追加 (HUD/status bar 表示用、 drag 中 true)。 **Widget state**: `PianoRollState.velocity_drag: Option<VelocityDragSession>` 追加。 session は `target_ids` (drag 起点 hit が selected に含まれれば selected 全部、 そうでなければ単一) + `anchor_velocities` (短 click 判定 + 「変化なし note 除外」 用) + `anchor_mouse` / `last_mouse` (note_drag と同パターンで winit release frame の pos 巻き戻し対策)。 note_drag (Move/Resize) と独立、 同 frame に両方 active にならない (pointer は press 時に grid か vel_area のどちらか)。 **絶対値 mode**: `velocity_from_y(py, vel_area)` で `(1.0 - (py - lane.y)/lane.h).clamp(0.0, 1.0) * 127` の formula で毎 frame 0..=127 を計算 (pointer 範囲外は clamp、 Live/Cubase 流)。 **Hit-test**: `velocity_bar_hit(visible, view, vel_area, cx, bar_width, tolerance=4.0)` で bar 中央 ±(bar_width/2 + tolerance) px 幅の hit zone (= 11px 幅 with default 3px bar)、 後勝ち (visible 順で前面、 note_hit と同 semantics)。 **drag 仕様**: lane 内 press で `velocity_bar_hit` が Some なら velocity_drag start (lane 余白 click は no-op = selection も変えない)、 drag 中は overlay の bar 高さが pointer.y で変化、 release で `dx.abs() + dy.abs() >= 3.0` のみ SetVelocity 発行 (drag<3px は誤操作防止 no-op)。 anchor velocity と new value が同値の note は updates から除外 (no-op Edit avoid)。 **描画**: `draw_velocity_lane` を `cached()` の **外** に移動して `velocity_override: Option<(&[NoteId], u8)>` 引数を追加、 drag 中は対象 ids の bar が new_vel で render されて即時 preview。 静的時の cost は ~visible_count rect commands (~50-100、 GPU で数千ns、 16ms 予算の 0.01% 未満)。 **Pending click 修正 (副次)**: `pending_click` を `grid.contains(pos)` で gate 追加 — 旧実装は vel_area / ruler / keyboard 等 grid 外の release でも `Vec::new()` selection clear を発行する latent bug があり、 vel_area click が selection を意図せず clear する症状を解消。 grid 内の空白 release は従来どおり selection clear (動作不変)。 **Example**: `crates/examples/piano_roll/src/main.rs` で `velocity_lane_h: 0.0 → 60.0` 有効化、 `make_set_velocity_edit(deltas: Vec<(NoteId, u8, u8)>)` factory 追加 (snapshot_inverse で undo 対応、 prev は dispatch closure で `velocity_snapshot: Vec<(NoteId, u8)>` から逆引き)、 SetVelocity arm 追加。 **trybuild**: `tests/ui/pass/basic.rs` に `NotesEditRequest::SetVelocity(_)` arm 追加で exhaustive 担保。 **unit test +14 件**: pure helper (velocity_from_y の lane top/bottom/clamp above/below/zero_height = 5 件、 velocity_bar_hit の hit/miss/overlap = 3 件) + integration (release で SetVelocity / drag<3px は no-op / multi-select 全部更新 / not in selection は単一更新 / lane disabled で起動せず / 空白 click で selection 不変 / response.velocity_dragging flag / unchanged velocity 除外 = 8 件、 計 8 + helper 6 = 14 件)。 cargo test workspace 全 ✅、 cargo clippy clean。 daw_01 conversation `#018 [Open]` → `[Replied]`。 **API 不変な部分**: 既存 caller の `PianoRollView` / `PianoRollStyle` / `Ui::piano_roll` signature は変更なし、 `match req` で SetVelocity arm を **追加** すれば良い (exhaustive match のため compile 時に漏れ検出される、 daw_01 daw_gui は path 依存再ビルドで compile error → SetVelocity arm 追加が必須)。 daw_01 follow-up: (1) `make_edit` の SetVelocity arm 追加 (`Vec<VelocityUpdate>` を model に書き戻す、 必要なら caller side で undo snapshot を保持して 1 commit にまとめる)、 (2) `velocity_lane_h > 0` を `PianoRollView` に設定して lane 表示を有効化 (visual feedback として velocity bar が見える)。 | ✅ 完了 |
| 63d | piano_roll の白鍵 / 黒鍵レーンの濃淡を鍵盤と整合 (daw_01 #017) | **背景**: piano_roll の grid は **黒鍵 row が白鍵 row より明るく** 描画されており、左の鍵盤 (`white_key 0.92` > `black_key 0.10`) と濃淡が逆転していた。 daw_01 user 報告。 **root cause**: `PianoRollStyle::default()` の `bg = (0.12, 0.13, 0.16)` (白鍵レーン) に対し `black_row_overlay = rgba(1.0, 1.0, 1.0, 0.04)` (白の半透明 overlay) を src-over 合成すると、 黒鍵 row 最終色 ≈ (0.155, 0.165, 0.194) が bg より **明るく** なる。 Ableton Live / Cubase / Reaper / FL Studio など主流 DAW は全て黒鍵 row を白鍵 row より暗くする慣習。 **修正**: `bg = (0.18, 0.19, 0.22)` (白鍵レーン = 明)、 `black_row_overlay = rgba(0.0, 0.0, 0.0, 0.25)` (黒系で暗くする)。 合成結果: 黒鍵 row ≈ (0.135, 0.143, 0.165)、 bg との差 0.045〜0.055 で視認可能。 階層: `ruler_bg(0.13) < velocity_lane_bg(0.16) < bg(0.18) < keyboard_bg(0.22)` で grid (note 配置領域) が piano_roll 内で最も明るい層に。 **doc 追記**: `bg` field doc に「`black_row_overlay` を src-over 合成した結果が `bg` より暗くなる値を選ぶこと」 不変条件を明記、 `black_row_overlay` field doc にも対応で「黒系の半透明色を使う」 を記載。 **unit test +1 件**: `default_black_row_is_darker_than_white_row` で src-over 合成結果 < bg を assert + 鍵盤 white_key > black_key の濃淡関係も同方向 (regression 防止)。 **API 不変**: `PianoRollStyle` 既存 caller は無修正、 daw_01 側コード変更不要 (gui_01 path 依存再ビルドのみで効く)。 daw_01 conversation `#017 [Open]` → `[Replied]`。 | ✅ 完了 |
| 63j | `Ui::arrangement` の ruler クリック / drag で playhead seek + loop range edit に snap 適用 (daw_01 #024) | **背景**: daw_01 PR7 の `Split (E)` shortcut は **playhead 位置で clip を 2 つに分割** する仕様だが、 `Ui::arrangement` widget は ruler 領域 click が **常に loop range edit (Start/End handle / Middle / NewRange)** に流れる旧設計だったため user が任意位置に playhead を置く手段が無く、 「再生中の任意のタイミングで E を押す」 か `loop_range` 端を drag するしかなかった (= ユーザビリティ不全)。 一般 DAW (Bitwig / Ableton / Reaper) は ruler click で即 playhead seek が標準。 daw_01 conversation #024。 **API 拡張**: `ArrangementEditRequest::SetPlayheadBeat(f64)` variant を `SetLoopRange` の直後に追加。 caller は (a) `view.playhead_beat = Some(beat)` 更新 (b) audio engine への seek IPC 送信に変換する責務 (caller 側 throttle / debounce は daw_01 の責務、 widget は raw に push)。 **press 振り分けの再設計**: ruler 内 press を **plain (`Shift` 非保持) → playhead seek**、 **`Shift` + ruler → loop range edit (Start/End/Middle/NewRange)** に分岐 (旧: ruler 内 press = 常に loop drag)。 multi-track 系 widget で Shift は加算選択用なので潰さない設計判断、 ruler は単一軸で Shift の他用途が無いので loop ops 専用 modifier として再利用。 業界踏襲: Reaper の Shift+drag = loop creation と同 pattern。 **新セッション**: `PlayheadDragSession { last_mouse_x, last_emitted_beat }` を `ArrangementState.playhead_drag` に追加。 press frame で session 起動 + `view.snap.snap_beat(raw, alt, zoom)` 適用 + `0.0` 以上 clamp で 1 度発火。 continuation frame は per-frame block で `last_emitted_beat` 比較 (1e-6 拍 epsilon) で同値抑制しつつ `SetPlayheadBeat` 連続発火 (`MoveClips` snap policy と同じ alt 直交 + `zoom_x_px_per_beat` Adaptive grid)。 release frame は session を `take()` して discard (commit-by-release 無し、 既に逐次発行済)。 **計算順序の整理**: `beat_per_px` / `zoom_x_px_per_beat` を関数頭 (lanes 定義直後) で 1 度計算、 press 振り分けと overlay 計算で共有 (旧: overlay 計算ブロック直前で計算 → press 側で snap が使えない)。 **loop range edit の snap 適用 (follow-up、 user 要望で同 commit に同梱)**: `LoopDragSession` に `last_alt: bool` を追加 (continuation で update / release で skip = ClipDragSession と同 pattern、 OS event 順序 race 対策)。 `compute_loop_drag_endpoints(ld, cur_beat_raw, snap, zoom) -> (start, end)` helper を新設、 overlay (drag 中 preview) と release commit (`SetLoopRange` 発火) で SSoT として共有 → 描画と commit が常に同一値で確定する。 endpoints 算出ルール: **Start drag** = moving 端点 (cur_beat) を grid に snap、 fixed 端点不変。 **End drag** = moving 端点 (cur_beat) を snap、 fixed 端点不変。 **Middle drag** = anchor_loop.0 を pivot として絶対位置 snap (Cubase / Live Move pattern と同じ) → delta を両端に適用 → duration 維持。 **NewRange** = press 時に `anchor_press_beat` を snap (caller 側責務)、 release 時に `cur_beat` を snap → 両端独立に grid 着地、 順序 `(min, max)` で正規化。 alt 押下中は `ld.last_alt = true` で snap bypass (`MoveClips` 直交 policy と同じ)。 **example (daw_prototype)**: `SetPlayheadBeat(beat)` arm を `SetLoopRange` arm の直後に追加、 `arr_view.playhead_beat = Some(beat.max(0.0))` + `last_action` 表示更新。 daw_prototype は audio engine を持たないので `playhead_beat` 更新のみで OK (実 daw_01 は seek IPC も発行)。 **trybuild + tests**: `tests/ui/pass/basic.rs` に `SetPlayheadBeat(_)` arm 追加で exhaustive 担保。 既存 `clone_drag.rs` / `alt_drag.rs` は wildcard `_ =>` で吸収。 **新 unit test +13 件**: playhead seek 5 件 (`ruler_plain_click_emits_set_playhead_beat` / `ruler_shift_click_does_not_emit_set_playhead_beat` / `ruler_plain_click_applies_snap_when_active` / `ruler_drag_emits_continuous_set_playhead_beat` / `lanes_click_does_not_emit_set_playhead_beat`) + loop snap 7 件 (`loop_endpoints_start_drag_snaps_moving_endpoint` / `loop_endpoints_end_drag_snaps_moving_endpoint` / `loop_endpoints_middle_drag_preserves_duration_with_snap` / `loop_endpoints_newrange_snaps_both_endpoints` / `loop_endpoints_newrange_normalizes_reversed_drag` / `loop_endpoints_alt_bypasses_snap` / `loop_endpoints_snap_off_returns_raw`) + Shift+ruler integration 1 件 (`shift_ruler_drag_emits_set_loop_range_with_snap`、 press → drag → release の 3 frame で SetLoopRange 1 度発火 + 両端 snap 確認)。 cargo test workspace 全 ✅ (361 + 13 + ...)、 cargo clippy clean (`shift_ruler_drag_emits_set_loop_range_with_snap` は `#[allow(clippy::too_many_lines)]`)、 cargo build --bin daw_prototype clean。 **API 影響範囲**: `Ui::arrangement` signature 不変、 既存 `MoveClips` / `ResizeClips` / `SetLoopRange` semantics 不変 (ただし `SetLoopRange` の発火値は snap 適用済になる)。 daw_01 caller は **breaking** 影響あり: `make_edit` の `match` に `SetPlayheadBeat(_)` arm 追加が必須 (exhaustive match のため compile error で漏れ検出)、 path 依存再ビルドで API を取り込める。 **副次効果 (要 user 確認)**: ruler の loop range NewRange 作成と Start/End/Middle handle drag は **すべて Shift 修飾必須に breaking change**。 旧 plain ruler drag = NewRange は不可、 既存 loop range の handle drag も plain では効かなくなる。 daw_01 / 既存 user の操作慣行を破壊する可能性あり、 「ruler のみ seek」 という #024 要望に最も忠実な design として採用。 user の visual verify で動作確認済 2026-05-08。 daw_01 conversation `#024 [Open]` → `[Replied]`。 daw_01 follow-up: `arrangement_view.rs::make_edit` に `SetPlayheadBeat(beat)` の arm を追加し、 `AppEvent::SeekToBeat(f64)` 等に変換 → `AppData` で `playhead_beat = Some(beat)` 更新 + audio engine `MainToChild::SeekTo` 送信。 audio engine 側は既存 `shared.playhead.store(...)` 経由で受け止める形が最小実装。 | ✅ 完了 |
| 63h | `Ctrl+Shift+drag` が rect select に化けて `CloneClipsIndependent` が emit されない bug 修正 (daw_01 #021) | **背景**: M14 Phase 63e (#019) で Ctrl+Shift+drag → `CloneClipsIndependent` 発火を実装したが、 user 報告で「Ctrl+Shift+drag するとアレンジ画面に矩形範囲表示 (cyan rect) が出て、 release で independent clone が発生せず selection 上書きだけが起きる」 bug が発覚。 daw_01 conversation #021。 **root cause**: `arrangement.rs` の rect select 起動条件が **Shift だけを見て Ctrl を見ていなかった** (line 2358 付近 `shift_press = primary_just_pressed && shift`)。 press 振り分け側 (line 1456) は `(!shift \|\| ctrl)` で「Shift があっても Ctrl があれば clip_drag 起動」 と書けていたので clip_drag セッションは正しく起動していたが、 続く rect select 側は Ctrl の有無を見ず **Ctrl+Shift+press で clip_drag と rect select が両方起動** していた。 結果 release 時に `CloneClipsIndependent` push (clip_drag 経路) **後** に `SelectClips` push (rect_select 経路) で selection が drag rect 内 clip 群で上書きされ、 user 視点「rect select に化けて clone が起きていない」 ように見える。 cyan rect overlay は `take_drag_rect_in_rect` の 1 度呼ぶと自動描画する仕様 (M8 Phase 33 設計) によるもの。 **修正**: rect select 起動条件 `shift_press` に `!ctrl` を追加して `shift && !ctrl` に絞る (`crates/ui/src/widgets/arrangement.rs` line 2358 付近、 1 行追加)。 これで (a) Ctrl+Shift+press on clip → clip_drag のみ起動 + cyan overlay なし + release で `CloneClipsIndependent` 単独 emit、 (b) Shift-only+press → 既存通り rect_select 起動 (`!ctrl` が true)、 (c) Ctrl+Shift+press on 空白 → どちらも起動せず no-op、 (d) shift_rect_active 経路 (Shift-only press 開始 → 途中で Ctrl 追加) は変えず継続。 案 B (rect select セッションでも `last_ctrl` 保持して release 時に振り分け直し) より単純で KISS。 **regression test +2 件 (`tests/clone_drag.rs`)**: (i) `arr_ctrl_shift_drag_does_not_trigger_rect_select` (Ctrl+Shift+drag に vertical 14px 入れて drag rect h>0 + clip と intersect する状況、 `last_clone_indep` is Some + `last_select` is None で SelectClips 混入を確実に検出 — 既存 `arr_ctrl_shift_drag_emits_clone_independent` は drag y=16 一定 で h=0 → `rects_intersect` が弾く偶然で SelectClips 検出できておらず「test 緑なのに実機 bug」 になっていた)、 (ii) `arr_shift_only_drag_still_triggers_rect_select` (Shift-only drag で `last_select` is Some + clip 100 含、 `last_clone_*` は None で受け入れ基準 (2) regression 防止)。 **API 不変**: `Ui::arrangement` signature / `ArrangementClip` / `ArrangementStyle` 全て無変更、 daw_01 側コード変更不要 (gui_01 path 依存再ビルドのみで効く)。 **commit 内訳の注**: 本 bug の `arrangement.rs` 修正は **Phase 63g commit f7013bd の差分に同梱され既に main に乗っている** (#022 と同 frame 編集だったため自然に bundle されたが、 commit message は #022 のみ言及)。 本 commit (Phase 63h) は regression test 2 件 + 本 plan.md 記述の追加で「#021 が修正されたことの証跡」 を残す。 daw_prototype で Ctrl+Shift+drag → 独立コピーが drop 位置に生成 + cyan rect overlay が出ない + 橙系 `+` ghost 表示を user 目視確認済 2026-05-07。 daw_01 conversation `#021 [Open]` → `[Replied]`。 | ✅ 完了 |
| 63i | `Ui::take_file_drop_in_rect` を `Option<DroppedFiles>` に拡張 (drop 位置を caller に提供、 daw_01 #023) | **背景**: daw_01 で audio file の drag&drop import を実装中 (`docs/plan_audio_clip.md` §3.1)、 仕様は「drop 座標 (x, y) → (start_beat, track) に解決 → AudioSource pool に追加」 で「ユーザーが track 2 に drop したら track 2 に配置」 を期待していた (Bitwig / Ableton / REAPER 流)。 ところが現状の `Ui::take_file_drop_in_rect(rect)` は `Option<Vec<PathBuf>>` のみ返すため caller は drop 位置を知る術がなく、 daw_01 は「最後に選択した track に配置」 で暫定動作。 内部では `DroppedFiles { paths, position }` を保持して rect 判定にも `position` を使っている (`crates/ui/src/ui.rs:1361`) ので、 戻り値で `DroppedFiles` 全体を渡すだけで position 公開できる。 **修正**: `Ui::take_file_drop_in_rect(rect: Rect) -> Option<DroppedFiles>` に変更 (旧 `Option<Vec<PathBuf>>` から)。 内部 logic は `self.file_drop.take()` で 1 行短縮 (paths 取り出し → DroppedFiles 全体取り出し)。 `HeavyCtx::take_file_drop_in_rect` も同形に追従、 `crates/ui/src/widgets/heavy.rs` の `use std::path::PathBuf` を `use crate::input::DroppedFiles` に置換 (PathBuf は heavy.rs 他で未使用)。 **example 更新**: `crates/examples/daw_prototype/src/main.rs` の file drop handler を `paths.iter()...` から `drop.paths.iter()` + `drop.position` で `last_action` を `"drop: <path> @ (x, y)"` 形式に拡張 (新抽象を次の機会に使う原則 = position フィールドの実用例を library 内に置く)。 **test 更新**: `crates/ui/tests/m8_integration.rs` の `file_drop_consumed_by_take_in_rect` を `paths_received: Option<Vec<PathBuf>>` から `drop_received: Option<DroppedFiles>` に変更、 paths.len() / paths[0] / position の 3 項目を assert に追加 (旧は paths.len() のみ)。 同 test の `file_drop_outside_rect_returns_none` も同様に変数名と型を更新。 **doc 更新**: `crates/ui/src/ui.rs:1358-1365` の doc comment に「戻り値の `position` は drop 直前の cursor 座標 (viewport 座標)。 caller は drop.paths と drop.position から (track, beat) など好きな解決を行う」 を追記。 `docs/plan_phase41.md` / `docs/plan_daw01_feedback.md` / `docs/history.md` (M8 Phase 32 行) も `Option<DroppedFiles>` に追従。 **API 影響範囲**: 既存 caller は `paths` 直接束縛箇所が `drop.paths` (or `drop.paths.iter()`) に書き換え必須 (compile error で漏れ検出される)、 daw_01 daw_gui は path 依存再ビルドで compile error → `arrangement_view.rs` の drop handler を `drop.paths` + `drop.position` に書き換えれば即 (track, beat) 解決可能。 **設計判断**: ユーザに同じ workaround を書かせる API は設計欠陥 (memory: feedback_pursue_best_practice)、 「最後に選択した track に配置」 暫定回避は daw_01 だけでなく将来 import 機能を持つ全 caller に boilerplate を強要するので library で吸収。 単一 workspace + Edition 2024 の利点を活かして全 example / test / docs を 1 commit で揃えた。 daw_01 conversation `#023 [Open]` → `[Replied]`。 | ✅ 完了 |
| 63g | shared clip の link glyph (`⇌`) が selection で消える bug 修正 (daw_01 #022) | **背景**: M14 Phase 63e (#019) で `share_group_color = Some(hue)` の clip にアクセント色 + name 左に `share_group_link_glyph` (`⇌`) を描画する仕様を入れたが、 user 報告で「共有 clip を click 選択すると link glyph が消える」 bug が発覚。 daw_01 conversation #022。 **root cause**: `arrangement.rs` の描画 pipeline は (a) cached 内 `draw_clips` が全 clip を描画 (link glyph 含む)、 (b) cached 外 `draw_selection_overlay` が selected clip だけ rect + 名前を **上から再描画** (selection 色で上書き) する 2 段構成だが、 `draw_selection_overlay` 側に link glyph 描画 path がなかったため、 selected な shared clip では cached 内で描いた link glyph の上に selection rect が被さり完全に隠れる。 cached 内/外を分けているのは selection 変化で `draw_clips` の cache を invalidate しないため (= 設計上の理由あり、 構造は維持)。 **修正**: `draw_clip` に `selected: bool` 引数を追加して描画状態を一箇所に統合 (DRY)。 (a) `selected = true` → fill = `clip_selected_fill`、 border = `clip_selected_border`、 text_color = `Color::rgb(0.10, 0.10, 0.15)` (selection 色の黄色 fill 上で読める黒)、 share の hue は ignore (selection 優先 = 仕様書 §1.3 通り)。 (b) `selected = false && share_group_color.is_some()` → HSL → RGB のアクセント色。 (c) それ以外 → `clip.color` or `clip_default_fill`。 **link glyph 描画は selected と独立**: `share_group_color.is_some()` なら描画、 link glyph と clip 名の text color は上記の `text_color` を共有。 `draw_clips` (cached 内) は `selected = false` 固定で全 clip を描画、 `draw_selection_overlay` (cached 外) は selected な clip だけ `draw_clip(.., selected = true)` で上書き再描画する形に書き換え。 これで cached 化を維持しつつ link glyph が selected/非 selected いずれでも shared なら描画される。 **API 不変**: `Ui::arrangement` signature / `ArrangementClip` / `ArrangementStyle` 全て無変更、 daw_01 側コード変更不要 (gui_01 path 依存再ビルドのみで効く)。 描画 pipeline の HeavyCtx output を直接観察する unit test 基盤がないため、 受け入れ基準 (selected/multi-select/解除) の検証は smoketest (visual) で実施 — daw_prototype で Ctrl+Shift+drag で共有 clip 作成 → click 選択しても `⇌` が左端に残ることを user 目視確認済 2026-05-07。 daw_01 conversation `#022 [Open]` → `[Replied]`。 | ✅ 完了 |
| 63f | `ArrangementResponse.clip_rects` (clip 右クリックメニュー実装用、 daw_01 #020) | **背景**: daw_01 で linked clip の「Make Unique」 操作 (refcount → content fork → 新 ContentId 採番) を arrangement 上の **右クリック → メニュー** で発火するため、 `Ui::context_menu_for(clip_rect, ...)` を caller 側で重ねる必要がある。 既存 `track_header_rects: Vec<(u32, Rect)>` (#005 で導入、 track header の rename overlay / context_menu 用) と完全平行な設計で `ArrangementResponse.clip_rects: Vec<(ClipKey, Rect)>` を追加するだけで済む小物 API 拡張。 **API 追加 (非 breaking)**: `ArrangementResponse` に `clip_rects: Vec<(ClipKey, Rect)>` field を `track_header_rects` の直後に追加、 `Default` 初期化は `Vec::new()`。 既存 `..Default::default()` 経由の caller は **無修正**、 直接 literal を組む caller も `clip_rects: Vec::new()` 1 行追加で fix。 **populate ロジック**: `pub fn arrangement` の末尾 (response return 直前) で `visible_tracks` を上から走査、 `draw_clips` と完全に同じ culling (row 縦範囲 + clip beat range) で `clip_to_rect` 結果を `(ClipKey, Rect)` ペアとして push。 順序は `(visible track index, clips slice 順)` = 上から下、 左から右の draw 順。 collapsed 親配下の子 clip は `visible_tracks` filter で除外、 完全 off-screen の clip も culling で除外、 部分カリングは full rect を返す (caller の `popup_rect_clamped_at` で popup の画面外 flip を吸収)。 `clip_to_rect` 計算は draw / hit-test / clip_rects で完全共有、 描画と response の整合性を構造的に保証。 **daw_prototype demo**: 既存 `track_header_rects` ループの直後に `clip_rects` ループを並列で追加、 `context_menu_for(*clip_rect, &["Make Unique", "Delete"], ...)` で 2 項目の右クリックメニューを起動。 Make Unique は `share_group_id = None` reset、 Delete は clip retain (memory: feedback_use_new_abstractions の「新抽象は次の機会に使う」 原則実践、 daw_prototype 自体は full content fork を持たないため簡略再現)。 **新 unit test +4 件**: `arrangement_response_default_has_empty_clip_rects` (Default 初期値) / `clip_rects_populated_in_visible_track_order` (2 tracks × 3 clips、 id / 順序 / x 昇順 / y 増加) / `clip_rects_excludes_collapsed_subtree` (parent collapsed の子 clip 除外) / `clip_rects_excludes_off_screen_clip_in_beat_range` (`view.start_beat=100` で beat 0 の clip 除外、 102 の clip 包含)。 cargo clippy clean、 cargo build --bin daw_prototype clean。 **API 影響範囲**: 既存 caller の `Ui::arrangement` signature は不変、 `ArrangementResponse` の field 追加のみ。 daw_01 caller は **path 依存再ビルドのみで** `resp.clip_rects` を使えるようになる (struct literal を直接組む caller がなければ source 修正不要)。 daw_01 conversation `#020 [Open]` → `[Replied]`。 daw_01 follow-up は仕様書 `docs/plan_clip_share_clone.md` §1.4 通りに `arrangement_view.rs` で `for (key, rect) in resp.clip_rects { ui.context_menu_for(rect, &["Make Unique"], ...) }` を組んで `AppEvent::MakeClipUnique(target)` 発火 (gui_01 から daw_01 へは commit しない)。 | ✅ 完了 |
| 63e | arrangement clip の共有コピー / drag-modifier-aware EditRequest + share_group_color (daw_01 #019) | **背景**: daw_01 で REAPER pooled MIDI 寄りの「共有コピー (linked clip)」 を導入予定 (`Song.clip_contents: HashMap<ContentId, ClipContent>` で notes を 1 実体共有、 piano roll 編集が同 source の全 clip に即時反映される)。 GUI 側に必要な機能を 1 commit で導入: (1) drag modifier に応じた EditRequest の出し分け (move / 共有コピー / 独立コピー)、 (2) drag 中の modifier-aware ghost preview (3 種視覚区別)、 (3) `ArrangementClip::share_group_color` フィールド追加 (共有 clip にアクセント色 + link icon)。 modifier 判定 = drag release 時 (drag 中に意図変更可、 REAPER / Live / Cubase 流)。 **API 拡張**: (a) `ArrangementClip` に `share_group_color: Option<f32>` 追加 (HSL hue `[0.0, 1.0)` 周期、 `None` で通常 clip、 `Some(h)` で widget が `(h, S, L_fill, L_border)` → RGB 変換した塗り / 枠 + name 左に link glyph を描画)。 caller は `content_id` を `[0.0, 1.0)` に hash して渡す想定 (refcount >= 2 のときだけ Some)。 (b) `ArrangementEditRequest` に `CloneClipsLinked(Vec<MoveClipDelta>)` / `CloneClipsIndependent(Vec<MoveClipDelta>)` 2 variant 追加。 `MoveClipDelta` shape を流用、 semantics は doc で明記: `from` = source identity (残置)、 `to_track` = 新 clip の配置 track、 `next_start_beat` = 新 clip の配置位置 (snap 適用済 absolute beat)。 daw_01 caller は (Linked) `from.clip` の `content_id` をそのまま共有する新 clip を追加 / (Independent) content を fork (新 ContentId 採番) する。 (c) `ArrangementStyle` に **計 11 field 追加**: clone ghost 6 (`clip_clone_linked_fill/border` 緑系、 `clip_clone_indep_fill/border` 橙系、 `clip_clone_badge_size/color`)、 share_group HSL parameters 4 (`share_group_saturation/fill_lightness/border_lightness/alpha`)、 link glyph 1 (`share_group_link_glyph: char` default `'⇌'`)。 全 tunable で theme 切替 / font 互換性に対応。 **Widget state**: `ClipDragSession` に `last_ctrl: bool` / `last_shift: bool` 2 field 追加 (`last_alt` と完全平行、 continuation frame で update / release frame で skip して winit 0.30 の `ModifiersChanged` が `MouseInput(Released)` より先に届く race を回避)。 **Hit-test / press 修正**: 旧 press filter `&& !shift` (Shift+drag は rect select に流す) を `&& (!shift \|\| ctrl)` に拡張 — `Ctrl+Shift+drag` は clone (Independent) 意図のため clip_drag に流す必要がある。 Shift only (Ctrl なし) は従来通り rect select に流す (regression なし)。 **Release dispatch**: `Move + last_ctrl + last_shift` → `CloneClipsIndependent`、 `Move + last_ctrl` → `CloneClipsLinked`、 それ以外 → 既存 `MoveClips`。 `ResizeLeft/Right` は modifier 関与せず常に `ResizeClips` (resize 中 Ctrl は意味なし)。 **Short-click demote**: 既存 `Move + Alt なし + dist < 4px` → `SelectClips` の三条件 AND は変更なし、 `last_ctrl` を demote 条件に入れない (Ctrl+click ≤ 4px は selection toggle、 Ctrl+drag ≥ 4px は clone — Ableton/Bitwig と同じ)。 **Alt 直交性**: Ctrl+Alt+drag = `CloneClipsLinked` + snap bypass (raw 位置 commit)、 Ctrl+Shift+Alt+drag = `CloneClipsIndependent` + snap bypass。 **描画**: `draw_drag_preview` を `(last_ctrl, last_shift)` で分岐、 ghost rect 塗り / 枠 / 左上 badge glyph (`⇌` / `+`) を切替 (rect が `badge_size + 4px` 以上のときのみ glyph 描画)。 `draw_clip` で `share_group_color = Some(hue)` のとき HSL → RGB で fill / border を上書き + name 左に `share_group_link_glyph` を 1 文字描画 + name を `clip_text_size + 2px` だけ右にずらす。 HSL→RGB 変換は CSS 仕様準拠の chroma-based 算出 (private fn `hsl_to_rgb`、 単一文字名 h/s/l/a/c/x/m は数学標記として `#[allow(clippy::many_single_char_names)]`)。 **daw_prototype**: `DawClip` に `share_group_id: Option<u32>` 追加、 `DawModel` に `arr_next_share_group_id: u32` counter 追加、 `arr_tracks_for_widget` で `Some(gid) → Some((gid as f32 * 0.618_034).rem_euclid(1.0))` の golden-ratio hash で hue 化 (隣接 group が色相的に十分離れる)。 `CloneClipsLinked` arm: source の現在 group_id を流用 or 新採番 → source / 新 clip 両方に同 group_id を assign (= source も share group に入って hue + link icon が出る)。 `CloneClipsIndependent` arm: 単純 deep copy (group_id = None)。 **trybuild + tests**: `tests/ui/pass/basic.rs` に `CloneClipsLinked(_)` / `CloneClipsIndependent(_)` の 2 arm を追加 + `share_group_color: None` を ArrangementClip literal に追加。 `tests/alt_drag.rs` の clip literal にも 1 行追加 (regression テストは無修正で全 16 件 pass)。 **新 integration test +8 件 (`tests/clone_drag.rs`)**: `Ctrl+drag` → `CloneClipsLinked` 発火 + source identity 確認 / `Ctrl+Shift+drag` → `CloneClipsIndependent` 発火 / `Ctrl release frame race` でも `last_ctrl` 保持で `CloneClipsLinked` 維持 / `Ctrl+Shift release frame race` でも `last_shift` 保持で `Independent` 維持 / `Ctrl+click` (< 4px) は demote → SelectClips (clone は発火しない) / `Ctrl+Alt+drag` → `CloneClipsLinked` + raw 位置 (Alt 直交) / 修飾なし regression → `MoveClips` のまま / `Ctrl+resize` → `ResizeClips` (Ctrl 無視)。 **API 不変な部分**: 既存 caller の `Ui::arrangement` signature は変更なし、 既存 `MoveClips` / `ResizeClips` semantics 不変。 daw_01 caller は **breaking** 影響あり: `ArrangementClip` literal に `share_group_color: None` 1 行追加 + `make_edit` の `match` に `CloneClipsLinked / CloneClipsIndependent` の 2 arm 追加が必須 (exhaustive match のため compile error で漏れ検出)。 path 依存再ビルドで API を取り込める。 daw_01 follow-up: 仕様書 `docs/plan_clip_share_clone.md` §8 に従って `Song.clip_contents` map / `Clip.content_id` / migration / D / Alt+D shortcut wire / context_menu「Make Unique」 を実装 (gui_01 から daw_01 へは commit しない)。 daw_01 conversation `#019 [Open]` → `[Replied]` (本 commit で確定通知の follow-up reply は別途記入)。 | ✅ 完了 |
| 61 | arrangement wheel zoom 修正 + viewport_key 内部 hash + `SnapMode::Bars` + piano_roll add_note pitch ceil (daw_01 #011 4 件 + #012 1 件 統合 1 commit) | **(a) wheel zoom 修正 (`#011 (1)+(3)`)**: `arrangement.rs:1903-1910` の Ctrl+wheel と Alt+wheel で `factor = (-dy * 0.005).exp()` を **`(dy * 0.0015).exp()` に変更** (符号反転 + 係数低減)。 wheel up = zoom in (一般 DAW 一致)、 1 ノッチで ~20% 変化 (Cubase / Live 同等)。 さらに **`SetZoomX` を絶対値送信に統一**: 旧設計は widget が factor 0.55..1.82 を直送り → daw_01 の `clamp(2.0, 400.0)` で必ず 2 に張り付き ruler 1〜100 圧縮を起こしていた (sema mismatch、 `SetTrackRowH` の M10 Phase 48 と同パターン外し)。 修正: `widget が new_zoom = (zoom_x_px_per_beat * factor).clamp(0.1, 10000.0)` を計算して送信、 doc コメント追記。 `arrangement.rs::tests::alt_wheel_emits_set_track_row_h` の expectation を新係数 `exp(-0.15) ≈ 0.8607` に更新。 daw_01 側 clamp(2, 400) は無修正で正しく動く。 **follow-up**: user 目視確認で「Ctrl+wheel なおっていません」 報告 → 原因は `daw_prototype` example の `SetZoomX` dispatch (L933) が古い factor semantic で `len_beats *= factor` していたこと。 example も絶対値 semantic に揃えて `len_beats = lanes_w / zoom` で逆引き保存に修正。 さらに user 要望「ズームの中心がマウスポインタじゃない」 → **mouse anchor zoom 追加** (Cubase / Live 標準): Ctrl+wheel は `SetScrollX` を同 frame で発行して `beat_at_mouse` を維持、 Alt+wheel は `SetTrackTop` を同 frame で発行して mouse 下の track 行が画面上で動かないようにする。 `arrangement.rs::tests::alt_wheel_emits_set_track_row_h` の「`!log.contains("SetTrackTop")` 」 assertion を「`log.contains("SetTrackTop")` 」 に反転 (anchor 調整で同 frame 発火)。 **(b) viewport_key 内部 hash (`#011 (2)`)**: `arrangement.rs` に `fold_arrangement_clip_hash(&[ArrangementTrack]) -> u64` 内部 helper を追加 (FNV-1a 64bit)、 全 clip の `(track.id, clip.id, start_beat.to_bits(), len_beats.to_bits())` を fold して viewport_key の 4 要素目として追加 + `b"arrangement_widget_v2"` → `v3`。 同根問題予防として **piano_roll.rs にも `fold_piano_roll_note_hash` を追加**、 全 visible note の `(id, start_beat, len_beats, pitch, velocity, lyric_arc_ptr)` を fold して 2 要素 outer tuple に wrap + `b"piano_roll_widget_v2"` → `v3`。 `lyric: Option<Arc<str>>` は **identity hash** (`Arc::as_ptr().cast::<()>()`) で扱い、 daw_01 VOICEVOX 編集の `Arc::from()` で必ず別 pointer になる前提に依存 (no-Clone 不変条件にも触れない)。 これにより caller の `data_generation` / `notes_generation` は **track / note 構成のみの責務** に整理され、 全 caller が clip detail を hash する boilerplate を強要されない設計に整理 (memory: feedback_pursue_best_practice)。 計算コスト: arrangement 100 clip = ~100ns、 piano_roll 5000 note = ~5μs (16ms 予算の 0.001-0.03%)。 **unit test +10 件**: arrangement +4 (stable / move / resize / id_swap)、 piano_roll +6 (stable / move / resize / pitch / velocity / lyric_arc_identity)。 daw_01 側コード変更不要 (gui_01 path 依存再ビルドのみで効く)。 **(c-pre) `#012` piano_roll add_note pitch 計算修正**: `piano_roll.rs:1184` の `pitch_f.round()` を **`pitch_f.ceil()`** に変更 (描画式 `y = grid.y + (pitch_top - pitch) * pitch_to_px` の逆引きは pitch_f ∈ (P-1, P] のとき P を返す = ceil、 round だと半行ぶん上にずれて行の下半分にカーソルがあると 1 pitch 下のノートが追加される bug)。 doc コメント追記 + 回帰防止 unit test (`piano_roll_insert_shortcut_uses_ceil_for_pitch`、 cy=215 で pitch 60 確認) + `TestModel.last_added_pitch` 計装。 daw_01 側 `daw_gui/src/view/piano_roll_view.rs:184` の同パターンは daw_01 単独で fix 済 (要望文補足)。 **(c) `SnapMode::Bars` + `SnapConfig.time_sig` (`#011 (4)`、案 A 採用)**: `crates/ui/src/snap.rs` に `SnapMode::Bars { count: u32 }` variant 追加 (1 bar = `time_sig.0 * 4 / time_sig.1` 拍、 4/4 → 4 / 3/4 → 3 / 6/8 → 3、 count = 0 は None で defensive)、 `SnapConfig` に `time_sig: (u8, u8)` field 追加 (default `(4, 4)`、 `DEFAULT` / `OFF` const も同 default)。 method signature 不変 (`snap_beat(raw, alt, zoom)` のまま) → caller の使い方変わらず、 既存 widget 内 6 箇所の呼び出し改修不要。 案 B (method 引数追加) を不採用にした理由: SnapConfig が data carrier として self-contained に保てる、 caller boilerplate 強要を回避。 daw_01 推奨も案 A。 既存 caller (`SnapConfig::DEFAULT` / `OFF` 経由) は **無修正で動く** (associated const は新 field 込みで再定義)、 影響は `tests/snap.rs` 7 箇所 + `tests/alt_drag.rs` 1 箇所の struct literal に `time_sig: (4, 4),` 1 行追加のみ。 **unit test +5 件追加**: 1@4/4 = 4 拍 / 2@3/4 = 6 拍 / 4@6/8 = 12 拍 / count=0 None / snap_beat 7.3 → 8.0 (1 bar boundary)。 **(d) daw_01 follow-up**: `daw_gui/src/view/snap.rs` の `piano_roll_snap_config` / `arrange_snap_config` の SnapConfig literal 2 箇所に `time_sig: app.song.time_sig` 1 行追加 (struct literal なので必須、 compile error で漏れ防止)。 SNAP_LABELS / choice_to_mode / mode_to_choice に Bars 系 (1bar / 2bar / 4bar) 追加は daw_01 単独で対応 (1/2bar は整数 count では表現不可、 実需要が出たら fraction Bars を別途検討)。 daw_01 conversation `#011 [Open]` → `[Replied]` に更新。 | ✅ 完了 |

**設計判断**:

- **`button_at_clicked` を新設、`button_at` の signature 変更は不採用**: `button_at` を `FnOnce(&mut Ui<M>) -> ()` に breaking 変更する案は menu item `m.item` の pattern と整合するが、既存 `button_at` callers (mixer / track_inspector / transport / examples / trybuild など何十か所) を 1 commit で全更新する破壊コストが過大。`button_at_clicked` を別 method として追加すれば「Edit を返したい場合」と「Ui 操作したい場合」の 2 用途で signature を分離でき、menu item の `&mut Ui` pattern と方向性は同じ。
- **builtin close button (`ModalStyle::show_close_button: bool`) は不採用**: daw_01 plugin_picker の Rescan + ✕ レイアウト都合と相反、style に hardcode された見た目を強制する。daw_01 が title row 内の ✕ 位置を完全制御できる方が良い (CLAUDE.md「要件にない変更を入れない」)。
- **`Ui::sync_modal(bool)` も不採用**: on_close 二重発火 + boolean ↔ popup state 二重管理を温存するので筋が悪い。「button click → close_modal → on_close fires → Edit (ClosePluginPicker) で boolean false」の一方向フローで完結する設計 (現状 modal API の意図) を維持。
- **`take_drag` / `take_double_click` にも masking を適用**: 一貫性 (3 関数のうち 1 つだけ漏れると次のバグ報告が必ず来る — `feedback_pursue_best_practice`「ユーザに workaround を強要する API は設計欠陥」原則)。コスト極小 (helper 1 関数 + 3 箇所に 1 行ずつ)。

**完了条件 (DoD)**:

- ✅ Phase 56 完了 (commit b25dff6)
- ✅ Phase 57 完了 (commit 5bdd24a)
- ✅ Phase 58 完了 (commit f3b9af9)
- ✅ Phase 59 完了 (commit c8ba70b、 user 目視確認済 2026-05-05)
- ✅ Phase 60 完了 (commit 5028175、 user 目視確認待ち 2026-05-05)
- ✅ Phase 61 完了 (commit 463f3dc、 user 目視確認待ち 2026-05-05)
- ✅ Phase 62 完了 (commit 1d808cf、 daw_prototype regression 目視確認済 2026-05-05)
- ✅ Phase 63a 完了 (commit 846dbd9、 unit test で popup primitive の clip_rect 経路を保証、 daw_01 daw_gui での #014 修正確認は user 側待ち)
- ✅ Phase 63b 完了 (commit e3a23e7、 wizardly-ride-852033 worktree 起源 / main 取り込み済、 user 目視確認待ち 2026-05-06)
- ✅ Phase 63c 完了 (commit 48abd8d、 daw_prototype で disclosure / multi-select / drag reparent 動作確認済 2026-05-06)
- ✅ Phase 64 完了 (本 commit、 piano_roll example で velocity lane drag 動作確認待ち 2026-05-07)
- ✅ Phase 63d 完了 (本 commit、 piano_roll example で 白鍵レーン > 黒鍵レーン の濃淡が鍵盤と整合するのを user 目視確認済 2026-05-07)
- ✅ Phase 63e 完了 (本 commit、 daw_prototype example で Ctrl+drag → linked clone (両 clip 緑系 hue + ⇌ icon) / Ctrl+Shift+drag → independent clone (badge + アイコン無し) / 修飾なし regression を user 目視確認待ち 2026-05-07)
- ✅ Phase 63f 完了 (本 commit、 daw_prototype example で arrangement clip 上の右クリック → "Make Unique" / "Delete" コンテキストメニューが表示される + 各項目クリックで `last_action` が更新されることを user 目視確認待ち 2026-05-07)
- ✅ Phase 63g 完了 (本 commit、 daw_prototype で共有 clip を click 選択しても `⇌` link glyph が左端に表示され続けることを user 目視確認済 2026-05-07、 daw_01 #022)
- ✅ Phase 63h 完了 (本 commit、 `Ctrl+Shift+drag` が rect select に化ける bug の regression test 追加 — 実装修正は Phase 63g commit f7013bd の arrangement.rs 差分に同梱済 / daw_prototype で Ctrl+Shift+drag → 独立コピーが drop 位置に生成 + cyan rect overlay が出ない + 橙系 `+` ghost 表示を user 目視確認済 2026-05-07、 daw_01 #021)
- ✅ Phase 63i 完了 (本 commit、 `Ui::take_file_drop_in_rect` の戻り型を `Option<Vec<PathBuf>>` → `Option<DroppedFiles>` に拡張、 caller が drop position を直接受け取れる → daw_01 audio file drop で track 解決可能、 daw_prototype example で drop @ (x, y) 形式の last_action 表示で動作確認待ち、 daw_01 #023)
- ✅ Phase 63j 完了 (本 commit、 daw_prototype で arrangement ruler を click → playhead 即移動 / drag で連続移動 / Shift+ruler drag で従来の loop range 作成 (両端 snap) / lanes click は既存挙動維持を user 目視確認済 2026-05-08、 daw_01 #024)
- ✅ Phase 60 visual verify で見つかった 4 件の追加修正 (本 commit に同梱):
  - **Alt+drag の真値ソース統一 (architecture 整理)**: 旧実装は drag overlay で `pointer.modifiers.alt` (frame snapshot)、 release commit で `nd.any_alt` (sticky alt) と **2 つの真値** を併用していたため、 (a) Alt を mid-drag で離した場合 overlay と commit が乖離 (overlay は snap 適用、 commit は raw)、 (b) OS event 順序で release frame に `pointer.modifiers.alt = false` が混入した場合 release で grid に飛ぶ — の不整合が出た。 修正: `ClipDragSession` / `NoteDragSession` の `any_alt` field を削除し、 overlay と release commit の両方が `nd.last_alt` (= continuation frame で update され release frame では update を skip して保持する値) を読む単一真値設計に統一。 加えて `last_mouse` も release frame で `pointer.pos != anchor` のときのみ update する条件付き更新に変更し、 `dist` 判定 (click 短縮) と delta 計算が同一の真値 (`nd.last_mouse`) を読むよう統一。 8 箇所の `[snap-debug]` eprintln も全削除。
  - **短 click 化の閾値設計を再構築 (UX 修正、 user 報告の本丸)**: 旧実装は **drag 距離 < 16px を一律「短 click」 に格下げ** して Move/Resize Edit を発行しなかった。 user が「Alt+drag で release すると grid に飛ぶ」「Alt なしで clip を伸ばしても release で元の長さに戻る」 「移動も同じ」 と報告した症状の本質は **「snap が再適用されている」 ではなく「短 click 化で commit されず clip / note が press 位置 (= 通常 grid 上の整数 beat) / press 時の長さに戻る」** だった。 16px 閾値は実用 DAW (Cubase / Live / REAPER は 3-5px 程度) に対して過剰で、 user の「ちょっとずらす / ちょっと伸ばす」 意図を一律 ignore していた。 修正: arrangement / piano_roll の短 click 化判定を **`Move + Alt なし + dist < 4px`** の三条件 AND に変更:
    1. **Resize は閾値関係なく常に commit** (resize handle 上の click は意味がないため)
    2. **Move + Alt 押下中は閾値 skip** (Alt は raw 微調整の明示意図)
    3. **Move + Alt なしは jitter 用 4px 閾値** (mouse jitter のみ ignore、 確実な drag は反映)
  - **delta-snap → 絶対位置 snap (off-grid anchor からの drag が grid に吸着するように)**: 旧実装は `view.snap.snap_beat_delta(raw_delta, alt, zoom)` で **delta だけを round** していたため、 anchor が grid 外に既にずれている (例: 前回 Alt+drag で +0.078 拍ずらした) 場合、 release してもずれが永久残る (raw_delta が grid 倍数なら snap 後も同じ delta を加算するだけ)。 user 報告「Alt+drag でグリッドからずらした後、 Alt なしの drag でグリッドに吸着しない」 の本質。 修正: `compute_clip_drag_beat_delta` / `compute_note_drag_beat_delta` 内部 helper を新設し、 **anchor 0 の編集対象端 (Move=start / ResizeRight=end / ResizeLeft=start) の絶対位置を snap → その差分 (`adjusted_delta`) を全 anchor に適用** する設計に変更。 これにより (a) 単一選択は最終位置が確実に grid 上、 (b) 複数選択は anchor 0 が grid 上に吸着しつつ相対関係維持、 (c) drag overlay と release commit が同じ helper を共有して描画 / commit が完全一致。 Cubase / Live と同じ「nearest grid alignment」 動作。
  - `crates/ui/tests/alt_drag.rs` を 16 test に拡張: 「Alt 持ち続け release」 「OS race」 「Alt なし長 drag は snap commit」 「Alt mid-drag 離して snap」 「Alt 押下 jitter (2px) でも commit」 「Alt なし jitter (2px) は click 化」 「Alt なし short (8px) drag は snap commit」 「**off-grid anchor からの drag は grid に吸着** (絶対 snap の ロック)」 を end-to-end カバー (× 2 widget = 16 件)。 `Ui::arrangement` / `Ui::piano_roll` の公開 API シグネチャは不変、 daw_01 側コード変更不要 (gui_01 path 依存再ビルドのみ)。
- ✅ Phase 59 visual verify で見つかった 5 件の追加修正:
  - **(a) `text_input_at` を controlled-only から uncontrolled-when-focused に**: `TextInputState` に `buffer_text: String` 追加、 `was_focused == true` 中は state.buffer_text を source-of-truth にして typing で mutate (旧設計は毎フレーム `text` 引数で working を reset していたため、 piano_roll の no-op `on_change` だと typing が反映されない bug)。 既存 controlled callers (daw_01 #013 rename 等) は各 keystroke で on_change → caller が model 更新 → text 引数 == buffer なので挙動完全互換 (全 299 test pass)
  - **(b) piano_roll example に IME enable/disable 処理追加**: `App.ime_enabled` field + `on_render` 末尾で `UiHost::ime_request()` の Some/None 切替を `WindowBackend::set_ime_allowed` + `set_ime_cursor_area` に dispatch (mixer.rs と同パターン、 piano_roll example が抜けていた)
  - **(c) piano_roll example に lyric edit demo モード追加**: `generate_notes(count)` で count ≤ 32 のとき pitch 60 (中央 C) に 0.5 拍刻みで連続配置 → default view 内に全 note 見える、 `PIANO_ROLL_NOTES=8` で歌詞編集 demo に最適化
  - **(d) lyric 描画を selection overlay より後に分離 + note 高さスケール font_size**: 旧設計は cached 内 `draw_notes` で lyric 描画 → selection の黄色 fill に隠れる bug。 新設計は `draw_lyrics` 独立 fn を cached の外で selection 後に呼ぶ。 font_size = `(note_h * 0.75).clamp(7.0, lyric_font_px)`、 default `lyric_font_px = 24.0` (max cap、 旧 9.0 固定) で zoom in / out で readable に自動スケール、 vertical center
  - **(e) `text_input_at_focused` 再表示時の state stale fix**: 編集 session 終了後 (lyric_editing = None) 同一 widget id を再度 show すると、 `state.last_focused == true` (前 session の終了状態) が残っていて gained_focus 検知が発火せず buffer / 全選択 reset が走らない bug。 `was_widget_visible_last_frame == false` 時に `state.last_focused = false` を強制 reset → 次の text_input_at で gained_focus 発火 → 既存 lyric 表示 + 全選択 (= L で再起動時に「分配済の 1 文字」だけが出て typing で即上書き可)
- ✅ `cargo build --workspace` / `cargo test --workspace` (**Phase 56 で 254+9+1+1=265、Phase 57 で text_input +2 = 256+9+1+1=267、Phase 58 で text_input +8 / ui +1 / text_metrics +3 = 268+9+1+1=279、Phase 59 で piano_roll +31 = 299+9+1+1=310、Phase 60 で snap +12 = 299+9+1+1+12=322、Phase 60 visual verify follow-up で alt_drag +16 = 299+9+1+1+12+16=338、Phase 61 で arrangement clip_hash +4 / piano_roll note_hash +6 / piano_roll insert_ceil +1 / snap Bars +5 = 310+9+1+1+17+16=354、Phase 62 で popup +8 = 318+9+1+1+17+16=362、Phase 63a で ui popup_clip +1 = 319+9+1+1+17+16=363、Phase 63b で snap +8 (DAW 業界標準 unit + Straight vs Bars 併存) = 327+9+1+1+17+16=371、Phase 63c で arrangement group +7 = 334+9+1+1+17+16=378、Phase 64 で piano_roll velocity drag +14 = 348+9+1+1+17+16=392、Phase 63d で piano_roll style invariant +1 = 349+9+1+1+17+16=393、Phase 63e で arrangement clone_drag +8 = 343+16+8 (workspace 単位 +9+1+1+17+16+8=401)**) / `cargo clippy --workspace --tests -- -D warnings` / `cargo test -p daw-ui-core --test no_clone_required` 全 ✅
- ✅ daw_01 conversation #015 / #016 を `[Open]` → `[Replied]` に更新 (`daw_01` ディレクトリでは commit しない、daw_01 側 user / 別 Claude が plugin_picker.rs / transport.rs 修正と同 commit で確定)
- ✅ daw_01 conversation #017 を `[Open]` → `[Replied]` に更新済 (commit c8ba70b)
- ✅ daw_01 conversation #019 を `[Open]` → `[Replied]` に更新 (Phase 63e、 本 commit で確定通知の follow-up reply は別途記入)
- ✅ daw_01 conversation #010 を `[Open]` → `[Replied]` に更新 (commit 5028175。 daw_01 側 user / 別 Claude が `Clip` schema / `snap.rs` を `daw_ui_core::SnapConfig` 経由に書き換えて確定)
- ✅ daw_01 conversation #011 を `[Open]` → `[Replied]` に更新 (本 commit。 daw_01 側 follow-up は `view/snap.rs` の SnapConfig literal 2 箇所に `time_sig` 1 行追加 + Bars 系 SNAP_LABELS 追加のみ、 (1)(2)(3) は無修正で効く)
- ✅ daw_01 conversation #012 を `[Open]` → `[Replied]` に更新 (commit 463f3dc。 piano_roll add_note の pitch 計算で `round()` → `ceil()` に修正、 daw_01 側コード変更不要)
- ✅ daw_01 conversation #013 を `[Open]` → `[Replied]` に更新 (commit 1d808cf。 popup の auto-flip + clamp 共通化で dropdown / menu_bar / context_menu_for を 1 箇所 helper 経由に統一、 daw_01 側コード変更不要 = gui_01 path 依存再ビルドのみで効く)
- ✅ daw_01 conversation #014 を `[Open]` → `[Replied]` に更新 (commit 846dbd9。 popup_layer entry で `current_clip = None` 退避を追加、 popup overlay は base scene の clip 制約から免除される不変条件を gui_01 側で吸収、 daw_01 側コード変更不要 = gui_01 path 依存再ビルドのみで効く)
- ✅ daw_01 conversation #015 を `[Open]` → `[Replied]` に更新 (commit e3a23e7。 `SnapMode::{Straight, Triplet, Dotted}` の係数を業界標準 (whole note = 4 quarter notes) に統一、 API 不変で daw_01 側コード変更不要 = gui_01 path 依存再ビルドのみで効く)
- ✅ daw_01 conversation #016 を `[Open]` → `[Replied]` に更新 (commit 48abd8d。 arrangement widget に group hierarchy + multi-select + drag reparent 追加、 SetTrackParent { tracks, parent, anchor_after } で parent 変更 + 挿入位置を 1 Edit に統合、 daw_01 follow-up は `arr_selected_track: Option<u32>` → `Vec<u32>` 化 + `arr_collapsed_groups` / `DawTrack::parent_id` 追加 + SetTrackParent arm 再構築の breaking 対応必須)
- ✅ daw_01 conversation #018 を `[Open]` → `[Replied]` に更新 (本 commit。 piano_roll widget の velocity lane に drag 編集を内蔵、 `NotesEditRequest::SetVelocity(Vec<VelocityUpdate>)` 1 variant 追加、 daw_01 follow-up は `make_edit` の SetVelocity arm 追加 + `velocity_lane_h > 0` 設定 = exhaustive match の compile error guidance あり)
- ✅ daw_01 conversation #017 を `[Open]` → `[Replied]` に更新 (本 commit、 旧 #017 の lyric editing は archive 済、 新 #017 は piano_roll の白鍵 / 黒鍵レーンの濃淡が鍵盤と逆転していたバグ報告。 `PianoRollStyle::default()` の `bg` / `black_row_overlay` 値を業界標準 (Live / Cubase / Reaper / FL Studio) に合わせて修正、 API 不変で daw_01 側コード変更不要 = gui_01 path 依存再ビルドのみで効く)
- ✅ daw_01 conversation #023 を `[Open]` → `[Replied]` に更新 (本 commit。 `Ui::take_file_drop_in_rect` 戻り型を `Option<DroppedFiles>` に拡張、 daw_01 follow-up は `arrangement_view.rs` の drop handler で `drop.paths` + `drop.position` を使って (track, beat) 解決 + `AppEvent::ImportAudio { paths, target_track, target_beat }` 発火に書き換え)
- ✅ Phase 59 user 目視確認済 (`PIANO_ROLL_NOTES=8 cargo run --bin piano_roll` で L → IME → "あいうえ" → Enter → 4 note 分配、 再 L で 1 文字 + 全選択、 全 OK 確認)
- 🔲 user 目視確認: Phase 56 (modal demo の wheel / close button)、 Phase 57 (text_input の BPM 入力欄でテンキー Enter commit)、 Phase 58 (text_input の全選択 / Ctrl+A / Shift+Arrow / Delete / cut/copy/paste、daw_prototype の rename UI で観察) — daw_01 側で動作確認待ち
- 🔲 Phase 60 user 目視確認: `cargo run --bin piano_roll` / `cargo run --bin daw_prototype` で drag overlay の Adaptive grid 吸着 / Alt 押下で raw / zoom で snap unit 切替 / release 時に「カクッ」と飛ばない
- 🔲 Phase 61 user 目視確認: `cargo run --bin daw_prototype` / `cargo run --bin piano_roll` で
  - Ctrl+wheel up 1 ノッチで ruler が ~20% 拡大 (zoom in、 1 ノッチで底値張り付きしない)、 wheel down で ~20% 縮小
  - Alt+wheel up 1 ノッチで track row が ~1.2 倍に広がる (zoom in)、 wheel down で ~0.83 倍 (zoom out)
  - clip drag move/resize → release 後に元位置 / 元 size の残像が 1 frame で消える
  - reorder track + clip move 組み合わせでも残像なし
  - piano_roll Insert キーを各視覚行の上端 / 中央 / 下端でカーソルを止めて押すと、 同じ pitch にノートが追加される (#012 ceil fix)
  - (Bars snap は daw_01 dropdown 4 件追加後に確認、 gui_01 単独では unit test 5 件 pass で確定)
- ✅ Phase 62 daw_prototype regression 目視確認済 (sidebar の preset dropdown 4 items が下開き、 menu_bar / context_menu も普段通り、 user 確認 2026-05-05)
- 🔲 Phase 62 daw_01 側 user 目視確認: `cargo run -p daw_gui` で piano_roll の snap dropdown (21 items) を開くと **上に flip して `Adaptive` まで全項目画面内に見える** こと、 arrangement の snap dropdown (上端 toolbar 配置) は元から下開きで regression なし
- 🔲 Phase 63a daw_01 側 user 目視確認: `cargo run -p daw_gui` で piano_roll の snap dropdown を click → popup が画面上に表示されること (修正前は一切出なかった)、 arrangement の snap dropdown も regression なし。 menu_bar / context_menu_for を `with_clip_rect` 内で開くケース (もしあれば) も regression なし
- 🔲 Phase 63c daw_01 側 user 目視確認 + caller 側更新: `daw_gui` の Track / app state を `arr_selected_track` → `arr_selected_tracks: Vec<u32>` に置換、 `arr_collapsed_groups: HashSet<u32>` 新設、 `Track::parent_id: Option<u32>` を追加 (model 側)、 SetTrackParent arm を「source remove → parent_id update → anchor_after 直後 insert」 の 3 段で arr_tracks を再構築する形で実装。 group hierarchy 動作 (folder track / multi-select drag reparent) を daw_01 daw_gui で確認
- 🔲 Phase 64 user 目視確認: `cargo run --bin piano_roll` で velocity lane (画面下端 60px) 内の bar を drag → release で velocity bar 高さが変わる、 multi-select で全 selected が同値、 drag<3px は no-op、 lane 余白 click は selection 不変
- 🔲 Phase 64 daw_01 側 user 目視確認 + caller 側更新: `daw_gui` の `make_edit` で `NotesEditRequest::SetVelocity(_)` arm 追加 (compile error が guide してくれる)、 `PianoRollView.velocity_lane_h: 60.0` 等で lane 表示有効化、 song の Note.velocity 反映確認

---

## 凍結 (M9+M10+M11 完了後の再評価対象)

下記 milestone は **凍結**。M9 (Real DAW Validation) + M10 (Arrangement 機能拡張) 完了の状態で、改めて優先順位を見直す。canonical な phase 番号は M9-M10 のみ。

### 凍結: 描画 / theming + アニメ + アイコン (旧 M9)

- theming システム (`Theme` struct、color palette / state スタイル)
- dark / light theme 切替
- アニメーション (`AnimatedValue<T>` / spring)
- vello 統合 + SVG アイコン
- gradient / shadow / blur

### 凍結: DAW 固有 widget — 信号処理系 (旧 M10)

- piano keyboard widget (単体、piano_roll と独立した「鍵盤だけ」widget)
- spectrogram (FFT + LOD)
- EQ curve / filter response (Bezier flatten 流用)

### 凍結: テキスト / 多言語 (旧 M11)

- IME 強化 (preedit cursor 精度、proportional font 対応)
- font fallback (CJK / emoji)
- 複数行 text editor
- 文字選択 (drag / triple-click / shift+arrow)
- i18n 基盤

### 凍結: OS 統合 (旧 M12 残り)

- file dialog 強化 (filter / directory chooser / recent files、現状 rfd 0.15 minimum)
- cursor 詳細 (Resize 8 方向 / Help / NotAllowed 等の full set、現状 Move / EwResize / Default のみ)
- fullscreen / borderless (DAW ライブモード等)

### 凍結: 開発体験 + リリース整備 (旧 M14)

- widget tree inspector (frame の widget 構造可視化、hit-test 視覚化)
- snapshot test framework (OffscreenRenderer + PNG diff、CI 自動化)
- widget catalog example
- rustdoc 充実 (user guide / tutorial / API doc)
- CI + benchmark regression (Github Actions / criterion baseline)

### 不採用

- **AccessKit** (旧 M6 Phase 20、2026-05-02 ユーザ判断): 個人 DAW プロジェクトのため対象外、再評価対象外。
- **baseview / 実 plugin host 検証 / プラグイン UI host integration sample** (旧 M13、2026-05-04 ユーザ判断): daw_01 で plugin editor は **別 stack** で動作中 (`Win32 / X11 embedding` 等)。gui_01 は「ホスト DAW のメイン UI」専用に割り切り、plugin renderer 用途は drop。
- **multi-window** (旧 M12、2026-05-04 ユーザ判断): plugin editor 別 stack なら gui_01 で複数 window を持つ動機は薄い (mixer 別 window 等のホスト UI 分離は単一 window + tab/split で代替)。drop。
- **`embedded_host` example** (M9 Phase 21 OffscreenRenderer の PNG snapshot は維持、plugin host 連携サンプルとしての立ち位置は drop): plugin host 用途を drop したことに合わせて、example は OffscreenRenderer の **動作確認** 単体例として位置付け直す (実 plugin に embed する手順は提供しない)。

---

## 設計上の不変条件

1. ライブラリ提供 API は **ユーザ Model 型に `Clone`/`PartialEq`/`Hash`/`Default` を要求しない**。差分検出は ID + プリミティブ末端値の hash でだけ行う。
2. メッセージ型は導入しない (Edit は `Mutate(Box<dyn FnOnce>)` または `Undoable { Arc<dyn Fn>, Arc<dyn Fn>, label }`)。`Application::Message: Clone` 伝染を構造的に防ぐ。
3. `derive` マクロは禁止 (Lens 等)。
4. ライブラリは **audio / IPC / プロセス間通信に一切関知しない**。Edit を返すところで責務を切る。
5. heavy() 以外でも viewport culling は前提 (1000 ウィジェット級は通常パスで耐える)。
6. `Ui<'a>` の `'a` で借用ライフタイムを統一し、GAT を使わない。
7. **波形ウィジェット固有**:
   - `WaveformSource` は借用のみ。`samples: &[f32]` の Clone は禁止。
   - LOD ピラミッドは派生データ (min/max ペア) で、生サンプルのコピーは禁止。
   - 再構築判定は `generation: u64` のみ。中身 hash や bitwise 比較は禁止。
   - 録音中の追記 (`valid_len` 拡大) はインクリメンタル拡張で扱う。
8. **Undoable Edit (M8 以降)**:
   - `Edit::with_inverse(label, forward, inverse)` は `Fn + Send + Sync + 'static` を要求するが、ユーザ Model 型に `Clone` を要求してはならない (closure 内でフィールドを set する形で書ける)。
   - 重い data の inverse capture でユーザに `Arc<...>` boilerplate を強要する状況になったら、library helper で吸収する (M9 Phase 44 で評価)。

---

## ビルド構成 (確定)

- **Rust Edition: 2024** (`rust-toolchain.toml` で固定済)
- **rust-version: 1.95** (workspace ルートで固定済)
- **依存**: `[workspace.dependencies]` で一元管理、メンバー crate は `<crate>.workspace = true` で参照
- **更新方針**: マイルストーン毎に最新安定版を確認、breaking change があれば追従

---
